# S01 Fixture Schema

**Asset ID:** `S01-FIXTURE-SCHEMA-v1.0` · Applies to `s01_synthetic_opportunity_cards.json`.

`demo_normal/demo_boundary`: `case_id` stable fictional identifier; `exception` synthetic input class; `handle_minutes` integer scenario observation; `rule_id` fictional rule or null; `confidence` 0–1 simulated score; `action` visible system state; `approval` named human authority. `constraints[]`: stable `id`, one of `access|authority|testability|value`, learner-facing `reveal`. `transfer`: stable ID, distinct industry/brief and facts required to reframe. `_metadata.synthetic` must remain true. No field represents selected-company data.

