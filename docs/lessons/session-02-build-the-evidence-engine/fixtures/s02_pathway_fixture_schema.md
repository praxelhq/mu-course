# S02 Pathway Fixture Schema

`pack_id` joins `content-packs.md`; `fields` names the role-authentic table columns; `values` contains fully synthetic rows in field order (null = supplied missing evidence); `calculation` is the required business method, not a solved answer; `source_ids` are stable fictional primary/contrast snapshot IDs to be rendered with explicit synthetic labels; `hidden` describes the sealed contradiction. Learners must create locators, transformations, formulas, caveats and decision implications. No row proves a selected-company fact.

