# S02 Demo — The Citation That Lies Without Lying

**Asset ID:** `S02-DEMO-v1.0` · **Duration:** 9 minutes · **Not an assessment input**

## Assets

Use the fictional `Northstar Support` memo, `data/s02_demo_source_snapshot.md` and a cached sheet. The memo reads: “Resolution improved 25%, proving the AI process caused better outcomes.”

## Script

1. Show the polished six-sentence memo/chart. Lock “forward / do not forward.”
2. Open citation `NS-SEC-2`: it defines resolved cases, but does not attribute causality.
3. Open raw values: baseline `20/100`, current `25/100`. Compute `+5 percentage points` and `+25% relative`; show why labels matter.
4. Reveal baseline is calendar Q1 and current is a four-week pilot: comparison scope differs.
5. Repair one chain live: question → exact locator → raw rows → normalized rates → visible formula → narrower claim → caveat → decision implication.
6. Ask “forward?” again. The output is less dramatic and more useful.
7. Close: “Research finds leads. A Claim Chain earns the right to claim.”

**Transfer cue (not solved):** show two new fictional hiring-market excerpts with a percent/percentage-point ambiguity and ask which Chain links are reusable. Do not calculate it; the assessed transfer uses a different pack and numbers.

Fallback is a six-frame PDF/text storyboard with formula cards. If spreadsheet fails, calculate on board. Never imply this synthetic case or causal result is real.
