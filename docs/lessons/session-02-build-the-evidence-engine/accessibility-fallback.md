# S02 Accessibility, Fallback and Evidence Parity

**Asset ID:** `S02-ACCESS-v1.0`

- Tagged HTML/text excerpts mirror PDFs; exact paragraph/table-cell locators replace visual highlighting.
- Spreadsheet route is keyboard operable with descriptive headers; printable extraction/calculation sheets and basic calculator are equivalent. No code, chart interpretation or color is required.
- Formula is accepted as symbols, spreadsheet expression or plain-language operations with inputs.
- Demo has captions/transcript and text table; peer reproduction may be written/asynchronous within the class window.
- Approved extra time preserves hidden pack and no-answer condition.
- Low bandwidth uses cached sources and CSV; outage uses numbered excerpts, sealed contradiction and paper transfer.
- Screen-reader users receive linear source then table order; duplicate/missing cells are announced in text.

Activate after accommodation request or a blocking service error over 60 seconds. Preserve prediction, v0, reveal ID, conflict classification, v1 diff, peer independent result and transfer. Staff import with source packet/hash. No learner loses marks for parser, network, authentication or visual-chart failure. LMS/device testing and institutional accommodation approval remain external gates.

