- 2026-09-04 · Room-composite Egress records AUDIO ONLY (`audio_only=True`, `INTERVIEW_EGRESS_AUDIO_ONLY=0` to restore video); the MP4 container and the reserved S3 key are unchanged · LiveKit bills composite egress at $0.02/min for video and $0.005/min audio-only, so across 516 students the recording was the single largest cost in the system — larger than every model call combined — and a measured 7-minute interview produced a 157 MB object, roughly 81 GB of S3 for the cohort against ~2.5 GB for audio. Nothing downstream reads the video: grading works from the transcript, and a human reviewing a flagged interview needs to hear it. Keeping the container and key means the reserve/commit path, `videoS3Key`, and DPDP erasure all work untouched · Rejected: switching to an OGG audio file type (would change the key extension and break the reserve/commit binding), and dropping the recording entirely (a flagged interview must be reviewable).
- 2026-09-04 · `INTERVIEW_MAX_ROOMS=15`, down from the default 30 · The default exceeded two vendor ceilings at once: LiveKit's Ship plan allows 20 concurrent AGENT SESSIONS (not participants — 1,000 of those), and Sarvam's Tier 1 Starter allows 20 concurrent streaming-STT websockets. At 30 the 21st student would have joined a live room whose agent could never be dispatched, which is exactly the "agent never joins" failure already seen once. 15 leaves headroom for reconnects, draining sessions and agent restarts, and caps throughput at ~90 students/hour — so a 516-student cohort needs staggering by section regardless · Rejected: 20 (no headroom for a reconnect that transiently doubles a session) and raising the LiveKit plan for one week.
- 2026-09-04 · STT and TTS run through LiveKit's `FallbackAdapter` with Sarvam first and Deepgram/ElevenLabs behind it, and `INTERVIEW_VOICE_PROVIDER=sarvam|legacy` pins one · Selection previously returned Sarvam whenever `SARVAM_API_KEY` was present, so the legacy pair — installed, keyed and paid for — was unreachable in production: the "fallback" only covered someone forgetting to set the key, never the vendor being down, rate-limiting or timing out, which is the failure that actually happens to a 516-student cohort on a Tier 1 Starter plan. The adapter fails over mid-session with per-provider retries, so a Sarvam outage costs a few seconds rather than the interview · Rejected: a startup-time health probe (proves nothing about the next fifteen minutes) and deleting `SARVAM_API_KEY` to force the fallback (loses the way back, and needs a human awake at the time).
- 2026-09-04 · The agent waits for the student to join (`ctx.wait_for_participant`, 120s cap) before greeting, and the interview budget rises to 20 minutes (`INTERVIEW_MAX_SECONDS`) · The first real student's interview shows the agent greeting an empty room at t=0 and her first word arriving at t=167 — 18% of the budget spent before the interview began, and the greeting itself thrown away, which is why she was greeted twice. Separately she spent 738 of 913 seconds talking and still reached only six questions, so RAG/MCP and all three set probes were never asked and the grader scored two-thirds of an evidence base that was never collected. Waiting recovers the dead air; 20 minutes buys roughly three more questions at her pace · The budget is env-tunable because each concurrent slot is held for its whole length, so 20 minutes cuts throughput from ~60 to ~45 students/hour against a 15-room cap · Rejected: 25+ minutes (throughput against the 8 Sep deadline) and greeting immediately with a longer time cap to compensate (pays for the dead air instead of removing it).
- 2026-09-06 · The dialog LLM is `llm.FallbackAdapter([LiveKit Inference, direct Gemini])`, both serving `gemini-3.6-flash`, keyed by a `GEMINI_API_KEY` reused from the praxy project · LiveKit Inference meters against a gateway credit that FOUR completed interviews exhausted; on 429 the agent still joined and Sarvam still transcribed, so three students sat through an interview where the interviewer never said a word and their answers were recorded against no questions. A mute interviewer is the worst failure mode this system has because it reads as the student's fault. Same model on both legs, so a failover changes who is billed and nothing the student can hear · Rejected: Anthropic for dialog (previously ruled out on cost and latency for a realtime voice loop, and unnecessary once a same-model fallback exists) and Sarvam-M via its OpenAI-compatible endpoint (unproven tool calling, and `end_interview` is load-bearing).
- 2026-09-06 · The sector map is digested like the blueprint; the resume is not · A 32,383-character system prompt is resent on EVERY turn, so a 28-turn interview costs ~340k input tokens and the sector map — up to 12,000 characters of extracted PDF, the single largest item — was most of that. The resume stays raw because it is half the size and its value is the specifics: "Product Manager at MoEngage" is what lets the interviewer ground a question in the student's own history, and a summary sands exactly that off · Each kind gets its own summariser instructions, because asking a research map the questions written for a Make blueprint produced neither well.
- 2026-09-06 · One explicit Gemini context cache per interview holds the instructions AND the `end_interview` schema; `INTERVIEW_PROMPT_CACHE=0` disables it · Implicit caching was MEASURED at a 0% hit rate on this prompt — five identical-prefix calls returned `cachedContentTokenCount: 0`, because a `systemInstruction` prefix does not qualify — while an explicit cache serves 99% of it. A 20-turn interview costs $0.065 uncached against $0.0075 cached including storage, so the cohort's dialog bill falls from ~$33 to ~$4. Gemini rejects any request combining a cache with `system_instruction`, `tools` or `tool_config`, so both live in the cache and the plugin bakes them out of every request; the tool SCHEMA is therefore cached while the agent still executes the Python function, and a test pins the two together because drift means the model calls something that does not exist and the interview cannot end · The cached leg LEADS a full chain rather than replacing it: the plugin decides per INSTANCE whether to bake `system_instruction` and tools out of a request, so the cached leg reads them from the cache while the uncached legs still receive them from the shared chat context. Collapsing to a single leg, as this first shipped, gave up the fallback for nothing · Deletion is best-effort and races the plugin's client shutdown; the TTL (interview budget + 5 min) is the real guarantee and the storage in between is a fraction of a cent.
- 2026-09-06 · Gemini leads the dialog chain BY DEFAULT; LiveKit Inference is kept as the last leg, and `INTERVIEW_DIALOG_PROVIDER=inference` flips it back · LiveKit Inference meters against an opaque gateway credit that ran to zero twice in one day, and each time it 429'd on EVERY turn rather than intermittently — the agent joined, Sarvam transcribed, and the interviewer never spoke. Four students sat through that; one typed "You can start asking questions" and left after two minutes. A FallbackAdapter only helps when the leader usually works, and leading with that endpoint made every question pay for a failed attempt plus retries before the fallback answered. This was briefly true only because an env var was set on the service, which meant one config reset would have restored the failing order — the default now carries it instead · A second vendor behind Gemini is still worth having, so it stays in the chain; nothing depends on it being up · Rejected: removing LiveKit Inference entirely (a same-model leg from a different vendor is free insurance while it is not leading).
