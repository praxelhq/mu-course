interface:
  display_name: "MU Quiz Builder"
  short_description: "Build mixed assessments and grader keys"
  default_prompt: "Use $mu-create-quizzes to create a mixed assessment, evaluator key, feedback, and calibration fixtures."
