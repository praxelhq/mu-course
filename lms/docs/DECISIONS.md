# DECISIONS.md — append-only log

Format: date · decision · why · alternative rejected.

- 2026-08-24 · The Aug 25 hosted-app submission collects separate idea, audience and user-flow briefs; the Aug 30 Make.com submission is team-owned and requires a Loom/live-run recording URL alongside the existing private evidence · These are the learner-facing minimums in the latest delivery brief while preserving evaluator evidence and privacy boundaries · Rejected: keeping the generic app writeup and file-only workflow recording contract.
- 2026-08-24 · App and workflow learners may create a new immutable submission version while their assignment gate is open; prior versions remain available for grading/audit and versioned revisions do not require a grant when the type opts into self-replace · Students need a correction path before the stated deadlines without destructive edits or instructor round-trips · Rejected: mutating the original row, deleting history, or requiring an instructor grant for every pre-deadline correction.
- 2026-08-24 · Production upgrades create `assess_s4_app_v2` and `assess_s5_workflow_v2`, superseding the published v1 contracts while preserving historical submissions and grades · Rejected: editing a published assessment version's schema or ownership in place.

- 2026-07-28 · Requirements source is the ten Drive build docs vendored in `docs/build/` (00–09); the repo root's older `docs/` course material (Mission Room concept, prior assessment system) is ignored as stale · The user directed this explicitly; the Drive folder supersedes it · Rejected: treating repo course docs as requirements.
- 2026-07-30 · `docs/build/SOURCE_OF_TRUTH.md` and `docs/build/10_sessions_3_5_redesign_brief.md` govern the Sessions 3-5 redesign; the current user brief supersedes the vendored Moxie/stocks lab and all root Session 3-5 packages · The delivered Industry Maps deck and frozen COT v3 establish the actual course arc: data, Lovable app, then Make.com workflow · Rejected: reviving the root RAG/method-package/control-room sequence or treating the existing S3 seed as current.
- 2026-07-30 · TrustMRR use is rights-gated before model processing, student distribution, derivative dataset generation, or product-clone selection · TrustMRR Terms dated 28 July 2026 require prior written permission for republication, AI grounding/evaluation, and TrustMRR-data-driven cloning use · Rejected: treating Sheet access as an unrestricted redistribution and AI-use license.
- 2026-07-30 · The user explicitly overrode the TrustMRR permission gate for this Course 1 project; proceed with AI processing, private LMS distribution, derivative teaching datasets, AI grading, and Session 4 selection while keeping row-level/derivative data out of the public repo, Praxy surfaces, and galleries · This is project-specific user authorization, not a general TrustMRR license · Rejected: blocking the build after the user supplied the governing authorization or treating it as permission for unrelated/public reuse.
- 2026-07-30 · New S4/S5 quiz versions score immutable item/option IDs, persist the displayed option order, and withhold score/key/rationale until the shared participating-section release time; legacy index quizzes remain explicit legacy contracts · Shuffling must never change answer identity, and an early section must not receive an answer key usable by a later section · Rejected: scoring shuffled array positions or returning an immediate result from every quiz.
- 2026-07-30 · Bootstrap and historical Session-2 setup create only missing shell/content rows and their own missing gates; they never rewrite authored page content or another session's release state · A routine roster/setup rerun must not relock Sessions 3–5 or overwrite their titles and summaries · Rejected: global gate resets and unconditional SessionPage updates in bootstrap scripts.
- 2026-07-28 · Realtime voice transport is LiveKit Cloud + LiveKit Agents (Deepgram → Gemini Flash → ElevenLabs) · Build prompt §2 and most of §6 pin LiveKit; a single leftover sentence in §6 says "OpenAI Realtime API" — treated as a drafting artifact · Rejected: OpenAI Realtime API.
- 2026-07-28 · Gate propagation via short-poll (3–5s) behind a client hook, not SSE · Identical UX at classroom timescales, no long-lived connection management on Railway, trivially load-testable; hook isolates a later SSE upgrade · Rejected: SSE for v1.
- 2026-07-28 · Near-duplicate detection uses content hash + Gemini embeddings (pairwise cosine within an assignment, in-memory) · Anthropic (the pinned grader) has no embeddings endpoint; the Gemini key already exists in the pinned voice stack · Rejected: adding a new embeddings-only vendor or pgvector in v1.
- 2026-07-28 · Role/section truth flows roster-row → Clerk publicMetadata (webhook pushes), never the reverse · First-sign-in Clerk accounts have empty publicMetadata; the reverse direction would overwrite imported roles · Rejected: Clerk-to-local mirroring.
- 2026-07-28 · Review-queue percentile trigger computed at queue render + batch finalise, not at grade time · Grade-time snapshots misflag early grades and miss late-context outliers · Rejected: grade-time percentile snapshot.
- 2026-07-28 · Sector claiming stays in the Praxel_MU_Sector_Tracker sheet for v0; the LMS stores each team's claimed sectorName (seeded from the 80-sector board) · Per 09_sector_board_80.md · Rejected: building a claiming UI in v1.
- 2026-07-28 · Grading eval bands are consistency checks, not accuracy ground truth · Bands are self-authored until MU digital-fluency calibration data arrives; instructor spot-validation of real early grades required before first batch-finalise · Rejected: treating a green eval as proof of grading accuracy.
- 2026-07-28 · Prisma pinned to v6 (6.x), not v7 · Prisma 7 removed `url = env(...)` datasource config (needs prisma.config.ts + driver adapters); v6 matches the pinned stack shape exactly · Rejected: Prisma 7 migration mid-scaffold.
- 2026-07-28 · Roster gate lives in `proxy.ts` (Next 16's middleware successor), not `middleware.ts` · Next 16.2 proxy files always run on the Node.js runtime, so Prisma works; Next 16 rejects a `runtime` segment config in proxy files (E1031), so no explicit runtime export is needed or allowed · Rejected: `middleware.ts` + `export const config = { runtime: 'nodejs' }` (the pre-16 pattern).
- 2026-07-28 · Off-roster Clerk accounts are flagged (privateMetadata.flaggedForDeletion + AuditLog), never auto-deleted · Deleting on webhook/proxy would destroy evidence and make a roster-data mistake unrecoverable; deletion stays a manual admin action · Rejected: `clerkClient.users.deleteUser` on unknown email.
- 2026-07-28 · Test-login backdoor requires NODE_ENV !== 'production' AND ENABLE_TEST_LOGIN, plus an instrumentation.ts boot assertion that throws if the flag reaches production · A single env flag is one misconfigured deploy away from an open door · Rejected: flag-only guard.
- 2026-07-28 · All server-side Clerk access goes through `lib/auth/` (dynamic imports), with explicit no-keys detection: proxy passes through and sessions resolve via the test-login cookie when Clerk keys are absent · Lets vitest/seed/Playwright and keyless local dev run without Clerk · Rejected: importing `@clerk/nextjs/server` directly in routes/components.
- 2026-07-28 · Migrations run in the web container's entrypoint (`docker-entrypoint.web.sh`: `prisma migrate deploy` then `node server.js`), with the Prisma CLI npm-installed globally in the runtime stage · Keeps deploy a single step; a failed migration fails the healthcheck and the previous deploy keeps serving; flat npm -g install avoids copying pnpm's symlinked node_modules into the standalone image · Rejected: Railway pre-deploy command (config drift across per-service files) and shipping the full builder node_modules.
- 2026-07-28 · Railway config-as-code is one file per service (`railway.json`=web auto-detected, `railway.worker.json` set via dashboard config path, `agent/railway.json` auto-detected at its root dir) · Railway has no multi-service config schema; web/worker share the `lms` root so only one can be auto-detected · Rejected: inventing a multi-service railway.json.
- 2026-07-28 · Worker image is single-stage, runs TS via tsx, `PLAYWRIGHT_BROWSERS_PATH=0` so Chromium lives in node_modules · Worker needs devDeps (tsx) anyway; no tsc build step to maintain; browsers-with-install survives any future stage copy · Rejected: tsc compile + prod-only multi-stage image.
- 2026-07-28 · Agent maps GEMINI_API_KEY → GOOGLE_API_KEY at boot (livekit google plugin reads the latter) · One canonical env name (GEMINI_API_KEY) across all three services · Rejected: requiring both names in Railway.
- 2026-07-28 · Gate resolution rule encoded once in `effectiveGateState`: open iff state==open OR (state==locked AND opensAt<=now); closed always stays closed (a manual toggle beats a schedule); missing row = locked · One function every consumer shares means no drift between hubs, dashboard, snapshot endpoint and console · Rejected: a background job materialising scheduled opens into state flips.
- 2026-07-28 · Per-student reopen is a new `GateException` table (forward-only migration `gate_exception`), unique (targetType,targetId,userId), honored by `resolveGate` when a userId is passed; an unexpired exception overrides both the target's own gate and its parent session gate · The schema had no exception mechanism; abusing AuditLog or ConfigKV as state would make resolution unqueryable and unindexed · Rejected: storing exceptions in ConfigKV/AuditLog, or widening Gate with a userId column (would break the per-section uniqueness).
- 2026-07-28 · Live propagation: `useGatePoll` polls `/api/gates/state` every 4s (paused while the tab is hidden), compares a sha1 content-hash `version`, and calls `router.refresh()` on change so server-component hubs re-render with fresh gates · Cheapest correct path to the ≤5s target; `ifVersion` short-circuit keeps idle polls to a tiny JSON body · Rejected: client-side gate rendering (duplicates resolution logic) and SSE (already rejected earlier).
- 2026-07-28 · Unlock Console close-with-pending confirmation is server-enforced: `/api/gates/set` refuses to close an assignment gate (single or session-bulk) while draft submissions exist in that section unless `confirmed:true` · A purely client-side confirm could be bypassed by a second console or a stale tab · Rejected: client-only window.confirm.
- 2026-07-28 · SSRF guard (`lib/net/safe-fetch`) validates resolved DNS addresses then fetches by hostname with manual, re-validated redirect hops (cap 3) — pragmatic v1, accepting the small lookup→connect TOCTOU window (documented in-module) · True IP-pinning needs a custom undici dispatcher; not worth it for a link checker + crawler in v1 · Rejected: custom Agent with pinned IPs.
- 2026-07-28 · Link-check rate limit is an in-memory per-user token bucket (~10/min) · Simplest thing that stops abuse from one browser; on multi-instance Railway the effective limit is N×10/min, acceptable for a HEAD-only checker · Rejected: shared-store (Postgres/Redis) rate limiting in v1.
- 2026-07-28 · Resubmission = a NEW Submission row at version+1 (history preserved, latest-version reads use orderBy version desc); contentHash = sha256 of canonical-JSON {fields, sorted files} written at submit time for U9 near-dup checks · Mutating rows would destroy grading history and break the matrix/version chips · Rejected: in-place update with a versions JSON column.
- 2026-07-28 · Image submissions are summarized as attachment notes in v1; vision input deferred · The grader is told an image exists (with its S3 key kept for a future vision pass) and instructed not to penalize the image itself; wiring image bytes through the grading call is overkill for v1 artifact kinds · Rejected: sending image blocks to the model in v1.
- 2026-07-28 · Model double-failure leaves the submission status 'grading'; the dead-letter row (queue `grade.submission.dead`) carries the failure and an admin re-enqueues via POST /api/admin/regrade (which resets status to 'submitted') · Resetting to 'submitted' automatically would make a poisoned submission retry forever and hide the failure; 'grading' + a dead letter is visible state U16 can surface · Rejected: auto-reset to 'submitted' on final failure.
- 2026-07-28 · Grading enqueue is best-effort at submit time: lib/queue.enqueueGradeSubmission swallows queue/DB outages (logged), the submission itself always succeeds, and admins re-enqueue via /api/admin/regrade · A queue hiccup must never lose a student's submission at the deadline · Rejected: failing the submit request when pg-boss is unreachable.
- 2026-07-28 · Grading model pinned to claude-sonnet-4-5 (env override ANTHROPIC_MODEL), temperature 0, one corrective retry on schema failure inside lib/ai/client.structuredCall · Sonnet-tier is sufficient for rubric grading at ~5x less cost than Opus-tier; the retry keeps transient format slips from burning a pg-boss retry cycle · Rejected: Opus-tier default and unlimited schema retries.
- 2026-07-28 · Dead-lettered grading jobs stay QUEUED in grade.submission.dead (no worker consumes them) so U16 can list (findJobs) and redrive them · A logging consumer would mark them completed and break redrive · Rejected: dead-letter log-and-complete consumer.
- 2026-07-28 · Review-queue candidate set = the latest grade of the latest submission version per owner (user, or team for team-based), provisional grades only; percentile ranking and batch finalise use the same set, and finalise excludes-and-returns unreviewed grades the re-run marks as outliers · Superseded v1 grades don't need review (the resubmission gets its own grade), and one shared candidate rule keeps queue/finalise/matrix/dashboard "latest" semantics identical · Rejected: queueing every provisional grade (would resurface superseded versions) and auto-finalising freshly-outlying unreviewed grades.
- 2026-07-28 · Gallery walls read through a single projection (`lib/galleries.getGalleryWalls`) that never selects the Grade relation — grade/score/confidence/promptLog data cannot leak into gallery payloads by construction; a deep key-scan test enforces it · Rejected: reusing dashboard/matrix helpers (they carry grade fields).
- 2026-07-30 · Supersedes the 2026-07-28 company-engagement rule: workflow blueprints, recordings, logs, and arbitrary legacy `Submission.files` are never gallery downloads, even when featured; legacy cards may show only safe caption/PNG, while versioned clone/sample actions require immutable policy allowlisting, owner consent, instructor approval, and safe evidence · Featuring is curation, not declassification, and raw automation artifacts can carry credentials or company data · Rejected: preserving the featured-item raw-file escape hatch.
- 2026-07-28 · Resubmission supersede MOVES the owner's GalleryItem to the latest graded version (featured+caption survive, screenshotS3Key resets for recapture) instead of creating a second item · One item per owner-chain keeps walls duplicate-free and preserves curation across versions · Rejected: item-per-submission with a superseded filter at read time.
- 2026-07-28 · Screenshot capture SSRF posture: appUrl validated through safe-fetch policy before any browser launch (blocked → screenshotS3Key sentinel 'blocked'); in-page subresources gated by request interception with per-request DNS lookup against isPrivateAddress (verdicts cached per host) · Same accepted TOCTOU window as lib/net/safe-fetch, documented in worker/jobs/screenshot-capture.ts · Rejected: proxying browser traffic through a vetting proxy in v1.
- 2026-07-28 · og:image fallback needs response bodies, so lib/net/safe-fetch gains safeFetchBytes (same policy loop, size-capped read) rather than any raw fetch in the worker · Keeps the 'all user-URL fetches go through safe-fetch' invariant intact · Rejected: a job-local fetch that re-implements the address checks.
- 2026-07-28 · Interview system prompt is stored as InterviewTurn 0 (speaker "system") at startInterview time · Resume (R17), the dialog model, and the grading job must all see exactly what the agent saw; Interview has no prompt column and turn 0 rides the existing unique(interviewId,turnNo) guard · Rejected: a promptLog-style column migration or rebuilding the prompt per call (submissions/config can change mid-interview).
- 2026-07-28 · Extra interview attempts are a new `InterviewRetake` table (forward-only migration `interview_retake`); startInterview consumes an unused grant atomically (updateMany where usedByInterviewId IS NULL) inside the create transaction · Attempt state must be queryable and race-safe; a grant admits exactly one start · Rejected: ConfigKV grants (unqueryable, unindexed) or bumping a counter on User.
- 2026-07-28 · Turn persistence: turnNo = max+1 inside a transaction, retried on unique-conflict; student answers additionally guarded in-transaction by "last turn must be agent" (DuplicateAnswerError 409) · unique(interviewId,turnNo) is the ordering guard; a concurrent double-submit keeps exactly one answer and never loses the first · Rejected: last-write-wins upserts or client-supplied turn numbers.
- 2026-07-28 (superseded 2026-09-07) · Every interview provider is optional-by-env with graceful degradation: no ElevenLabs → text-only questions; no Deepgram → typed answers only (audio submit 503s); no Gemini → typed 503 unless INTERVIEW_DEV_SCRIPTED=1 enables a deterministic question bank from ConfigKV interview_script · The whole loop must run locally with zero keys; the scripted/text path is a dev fallback, NOT a product mode (audio is primary when keys exist) · Rejected: hard-requiring keys in dev or silently faking model output without an explicit env flag.
- 2026-07-28 · Interview.rubricScores keeps the seeded flat shape ({industry_command: n, …, total}) with rationales/flags nested beside it · Seed rows (iv_001/iv_002) and any existing readers already use the flat shape; the instructor page reads both · Rejected: migrating to the Grade-style {score, rationale} nesting.
- 2026-07-28 · POST /api/interview/start returns the student's existing LIVE interview (resume) instead of 409; only non-live prior attempts exhaust the attempt · A reload/drop must never burn the single attempt (R17); the session-layer guard stays strict so U13 shares it · Rejected: treating any prior row as exhausted at the route level.
- 2026-07-28 · TTS mp3s are written server-side via lib/s3.putObject (small, server-generated bytes) — a documented exception to "the app tier never proxies file bytes", which governs USER file uploads/downloads (those still use presigned URLs; answer clips upload browser→S3 directly) · Rejected: an ElevenLabs→S3 relay worker job (adds a queue hop to a latency-sensitive turn).
- 2026-07-28 · Realtime concurrency guard counts Interviews (status live + transport realtime + lastSeenAt within 90s) against INTERVIEW_MAX_ROOMS (default 30); over-capacity token requests get 429 {waiting:true, activeRooms} and the client polls every 10s — no persisted WaitingRoom table or strict FIFO position · Heartbeats (state polls + agent-turn posts, writes throttled to 30s) make abandoned rooms self-expire; a queue table would add state to reconcile for marginal fairness at this cohort size · Rejected: a WaitingRoom entity with FIFO positions.
- 2026-07-28 (superseded 2026-09-07) · In-place degradation: the client POSTs /api/interview/fallback (owner-or-404, audited) to flip transport realtime → turnbased-fallback on connect timeout (~8s), disconnect, or sustained-poor quality (>10s); the SAME interview continues over the U12 loop, and agent-turn posts are refused 409 after the flip so a straggling agent can never interleave with the turn-based transcript · One interview, one ordered transcript, attempt never burned · Rejected: ending the realtime interview and granting a retake, or letting both writers share the live transcript.
- 2026-07-28 · The Python agent owns Egress (room-composite audio-only → interviews/{id}/room.ogg, started at job start when S3 env is present, stopped before agent-complete which carries the key); the LMS only validates the key namespace and stores it · The agent already holds LiveKit server creds; the web tier minting egress for a room it never joins would duplicate config and add a failure mode · Rejected: web-tier egress orchestration.
- 2026-07-28 · Agent internal endpoints (agent-turn/agent-context/agent-complete) authenticate via X-Agent-Token compared constant-time (sha256 both sides + timingSafeEqual) against AGENT_INTERNAL_TOKEN; 503 when the env is unset, 401 on mismatch · A shared secret is sufficient for one first-party worker; hashing first makes the comparison length-independent · Rejected: per-interview signed tokens or mTLS in v1.
- 2026-07-28 (superseded 2026-09-07) · Realtime availability is decided SERVER-side: the token endpoint answers 503 {realtimeUnavailable:true} when LIVEKIT_* is absent and the client simply tries it first — no NEXT_PUBLIC_REALTIME_ENABLED flag · One source of truth; zero-key local dev degrades to turn-based with no client build differences · Rejected: a public env flag the client must keep in sync.
- 2026-07-28 · Quiz submit idempotency is the DB unique constraint (quizId,userId) alone — one row insert, catch P2002, answer "duplicate" WITH the original result; no pre-check SELECT, no transaction · Survives 60 writes/s and Promise.all double-submits with zero hot locks; a pre-check would race anyway · Rejected: SELECT-then-INSERT in a transaction.
- 2026-07-28 · Mid-close grace: a quiz submit is accepted for GRACE_SECONDS=120 after the gate flips closed, decided from Gate.closedAt server-side (new lib/gates resolveGateDetail keeps the closedAt read inside lib/gates) · A client-supplied startedAt is unfalsifiable; the instructor's close mustn't eat in-flight answers · Rejected: trusting a client timestamp.
- 2026-07-28 · Diagnostic isolation (R24) is enforced at the query layer of the single student repository module lib/quizzes: retrospective reads filter quiz.isDiagnostic=false in the WHERE clause, and no student-facing return type carries an isDiagnostic field at all; the live taking path deliberately does NOT filter, and revisiting any already-taken quiz shows the same "already submitted" card — so nothing is detectable in either direction · Instructor reads live in lib/quizzes/instructor, a separate import path · Rejected: filtering in pages/routes, or a serializer that strips the flag late.
- 2026-07-28 · PCI formula implemented VERBATIM from the frozen methodology §5 — (pointsReceived ÷ (100×(teamSize−1))) × teamSize, 40/60 combine toward checkpoint 2, clip to [0.70,1.20] strictly AFTER combining · Under the literal formula a perfectly even split across n−1 teammates computes to n/(n−1) (≈1.14–1.20, absorbed by the clip band) and the doc's "equal share → 1.0" anchor corresponds to receiving 100·(n−1)/n points; the frozen doc wins over its own prose · Rejected: renormalizing the multiplier to make even splits land on exactly 1.0 (would silently change the frozen formula).
- 2026-07-28 · PCI-multiplied components clip to 100 AFTER the multiply (value chain map, workflow) — §8 requires every component on a 0–100 scale before weighting, and a 1.2 PCI on a strong team score would otherwise exceed it · The clip and the pre-clip product are both shown in the line's detail text · Rejected: letting weighted contributions exceed the component's nominal maximum.
- 2026-07-28 · Workflow §3 parts v1 BRIDGE: the seeded workflow rubric is the generic 4-dim (functionality/craft/relevance/verification-evidence, 0–10 each), so lib/scoring/assemble derives usefulness(0–30)=(craft+relevance)/20×30, execution(0–20)=functionality/10×20, ownership(0–10)=verification-evidence/10×10 until a workflow-specific rubric is configured; ownership stays OUTSIDE the PCI multiplication ("applied to that specific student only") · Rejected: blocking the grade line on a rubric migration.
- 2026-07-28 · Peer contribution standalone line normalizes to 0–100 pre-weight (mean of all 1–5 ratings × 20) like every other component, arithmetically identical to §5's 0–10 score at the 10% weight · Rejected: carrying a 0–10 line item through a formula whose other six components are 0–100.
- 2026-07-28 · Peer checkpoint submission: active checkpoint from ConfigKV peer_checkpoint {active:1|2|null} (seeded active:2); resubmission is a plain upsert on unique(checkpoint,reviewer,reviewee) — overwrite allowed while active, frozen when active flips away; all validation failures (sum≠100, self, missing teammate, bad ratings) are 422 · Rejected: per-checkpoint open/close timestamps or submission locking in v1.
- 2026-07-28 · Portfolio v1 heuristics (until U16): completeness = fraction of the 6 seeded artifact types with a graded/finalised submission ×20; narrative by length (≥200 chars→25, ≥80→15, present→8); validations JSON [{kind:'external'|'peer'}] score 5/ea cap 25 and 3/ea cap 15; evidence integrity expects lastCrawl {checkedAt, links:[{url,ok,status?}]} → ok-fraction ×15, missing crawl → 0 with "no crawl yet" · Rejected: hard-failing the portfolio line on absent crawl data.
- 2026-07-28 · Grade line ownership is structural: /grades is a parameterless server component that passes requireUser().userId into getGradeLine — there is no route or query param that could name another student · Rejected: a /api/grades/[userId] endpoint with an ownership check.
- 2026-07-28 · Portfolio links JSON keeps the seeded object shape `{external: [{label,url}], …}`; the student API rewrites only the `external` array and preserves unknown keys (the seeded `submissions` list) · One canonical shape, zero data loss on first save over a seeded row · Rejected: migrating links to a bare array.
- 2026-07-28 · v1 has NO student "request peer validation" flow — validations (external AND peer) are recorded by instructors/admins on /instructor/validations (append to PortfolioEntry.validations {kind,by,note,at}, AuditLogged) · The validation source of truth in v1 is the instructor witnessing it; a request/approve loop is future work · Rejected: a student-facing request queue.
- 2026-07-28 · Portfolio crawl gathers URLs schema-driven: portfolio external links + every link-KIND field of the student's submitted submissions (appUrl/githubUrl/skillLink discovered from submissionSchema, never hardcoded); probes HEAD-first (GET on 405/501) through lib/net/safe-fetch with per-student concurrency 3; a blocked (private-IP) URL records ok:false WITHOUT status and without any connection · lastCrawl contract {checkedAt, links:[{url,ok,status?}]} is shared verbatim with lib/scoring/assemble (ok/total×15) · Rejected: a hardcoded field list and raw fetch.
- 2026-07-30 · Supersedes the 2026-07-28 DPDP prompt-log decision: the access bundle never selects or serializes Grade/evaluator rows, prompt logs, confidence, rubric/answer material, raw workflow logs, TrustMRR rows, or object-store locators; versioned fields and evidence metadata require the immutable `exportPolicy`, while appeals are a separately sanitized history · The redesigned privacy boundary is narrower than the old generic ORM export and must remain safe even under a mis-authored contract · Rejected: carrying model prompts or scores into the bundle for context.
- 2026-07-30 · The redesigned DPDP access bundle declares top-level `contractVersion: 2` and downloads as `dpdp-export-v2-<userId>.json`; v2 intentionally removes the sensitive evaluator, answer, score, raw-log and object-locator fields from the old shape · Legal-response and archival consumers need an explicit migration boundary without reintroducing privacy-removed fields · Rejected: serving the replacement schema under the unversioned legacy filename or restoring unsafe v1 fields for compatibility.
- 2026-07-28 · DPDP export carries peer reviews GIVEN by the student only — reviews RECEIVED are the reviewers' personal data (their allocations/opinions) and are excluded; other students appear only as opaque ids, never name/email (test-enforced by scan) · Rejected: exporting received reviews.
- 2026-07-28 · DPDP delete removes the user ROW entirely (roster re-import can recreate the account) in one FK-safe transaction, flags the Clerk account best-effort, and KEEPS one AuditLog row whose summary carries the email + per-table deletion counts — the legal record of the erasure must outlive it · Rejected: anonymising the row in place, or deleting the audit trail of the deletion itself.
- 2026-07-28 · Grading dead letters are listed straight off pg-boss (`findJobs('grade.submission.dead', {queued:true})` on the shared PgBoss singleton) on /admin/costs, with Retry → the existing POST /api/admin/regrade; a queue outage renders an empty list, never a broken page · Rejected: raw SQL against pgboss.job (couples us to pg-boss's internal schema).
- 2026-07-28 · Praxy export stub (/api/praxy/export, instructor+) returns artifacts (type/title/links/submittedAt/featured from graded-or-finalised latest versions + portfolio links) and badges (company-sign-off with evidence flag, interview-completed when status=graded — escalation resolution flips to graded so one check covers both, external-validation count) — NEVER totals/scores/rubric/confidence/PCI/quiz data; tests/praxy-export.test.ts deep-scans keys AND values for forbidden terms · Rejected: carrying any status or score field "for context".
- 2026-07-30 · Supersedes the 2026-07-28 Praxy artifact shape: `/api/praxy/export` now declares top-level `contractVersion: 2`, and each policy-safe artifact carries `type/title/version/fields/submittedAt/featured`; arbitrary schema links and portfolio links are not restored · The explicit version lets the Praxy consumer migrate to immutable allowlisted fields without mistaking the response for the old links-array contract · Rejected: silently changing the payload or preserving broad v1 link projection at the expense of the new publication boundary.
- 2026-07-28 · Playwright covers the gate-flip live-propagation demo via an API-driven flip + student page reload (poll-based waiting on the 4s client poll is flaky in CI); the in-browser optimistic flip is asserted separately on the Unlock Console · Rejected: sleeping through poll cycles in CI.
- 2026-07-28 · k6 scripts authenticate with the forge_test_user cookie via the k6 cookie jar and are guarded by banner comments: dev/staging with ENABLE_TEST_LOGIN=1 only, never production (a production build refuses both the cookie and the flag) · Rejected: scripting Clerk logins for load tests.
- 2026-07-28 · DPDP erasure scopes deletion to the person's INDIVIDUAL work: individual submissions (teamId null) + their grades/gallery items are hard-deleted, but a jointly-produced TEAM artifact (value-chain-map/media/workflow, stored under whichever member clicked submit) is REASSIGNED to a surviving teammate (or deleted only if the erased user was the team's last member) so erasing one member never zeroes four-to-five teammates' §1/§2/§3 grades; the deletion counts include reassignedTeamSubmissions and the AuditLog + best-effort Clerk flag are unchanged · Rejected: hard-deleting every row where userId matches (destroys the team's only artifact and every teammate's grade line, indistinguishable from "the team never submitted").
- 2026-07-28 · Presigned upload PUTs bind the client-declared exact byte length into the signature (PutObjectCommand ContentLength = the validated declared size, passed as presignPut's maxBytes) so the per-type size cap is enforced at S3, not client-advisory only — the browser's fetch PUT sends Content-Length = file.size which must match · Rejected: signing only Bucket/Key/ContentType (a caller declaring sizeBytes:1 then uploading gigabytes was accepted), and createPresignedPost (would change the PUT uploader + presignPut return shape).
- 2026-07-28 · Grading total is ALWAYS recomputed server-side from the validated (0–10) per-dimension scores in applyPolicyFlags — the model's self-reported `total` is never persisted (it was previously trusted except on dead links); a hallucinated total would be scaled ×2.5 downstream and inflate the final grade past the component scale · Rejected: constraining total in Zod alone (still trusts the model's arithmetic when it happens to be in range).
- 2026-07-28 · Assignment submission version counter is per-OWNER: individual types count by userId, team-based types count over the whole team (OR userId/teamId, mirroring the read-side mineOrTeam filter) so a resubmission by a different teammate continues the team's sequence instead of resetting to v1 (which every version-desc "latest" consumer would then read stale) · Rejected: a flat per-user counter for team types.
- 2026-07-28 · Typed interview answers are gated server-side (POST /api/interview/answer refuses a text-only body with 400 unless NODE_ENV!==production OR NEXT_PUBLIC_INTERVIEW_TEXT_MODE=1 OR INTERVIEW_DEV_SCRIPTED enabled) — a graded viva in production must be spoken, never typed with no audio artifact; mirrors the isTestLoginEnabled env-gating pattern · Rejected: accepting `text` unconditionally (a student could complete the interview by POSTing typed answers).
- 2026-07-28 · Interview resume regenerates the next question when a crash landed between answer and next-question (live interview, no pending question, last turn is a student turn): POST /api/interview/start calls the idempotent nextQuestion in the resume branch (completing on done) so /answer never 409s forever on a transcript that ends on a student turn (R17) · Rejected: returning getInterviewState as-is (leaves the room with nothing to answer and the attempt wedged until the 12-min budget).
- 2026-07-28 · Submissions stuck at status 'submitted' past ~10 min (grading enqueue that silently failed at submit time, so the job never entered pg-boss and never dead-letters) are surfaced on /admin/costs via lib/submissions.listStuckSubmissions with the existing Retry → POST /api/admin/regrade; enqueue stays best-effort · Rejected: leaving the observability gap between "job dead-lettered" and "job never enqueued".
- 2026-07-30 · Versioned scoring, portfolio, gallery, screenshot, Praxy and DPDP consumers parse strict immutable policies and fail closed; mutable slugs/schema flags remain only inside explicit legacy branches · A malformed or missing bound contract must not silently inherit behavior from an older assignment definition · Rejected: hard-coded new-session slugs or permissive policy defaults.
- 2026-07-30 · Public projection requires both active canonical-owner consent and an independent instructor approval fingerprint; pending/unsafe V2 leaves a safe V1 visible, while owner or instructor revocation removes the whole visible chain immediately · This preserves classroom continuity without allowing a revoked artifact to reappear through fallback · Rejected: instructor-synthesized consent and newest-submission-wins publication.
- 2026-07-30 · App screenshots are content-addressed and mutable public URLs are server-rechecked through the safe-fetch boundary; a changed preview or external-content marker withholds the reviewed action until instructor re-approval · Lovable subdomain policies use explicit boundary-safe wildcard rules, and Make clone actions accept only official Make hosts plus scenario-sharing paths · Rejected: client-supplied fingerprints, raw fetch, suffix-confused host matching, or permanent trust in a dynamic link.
- 2026-07-30 · Team workflow nominations are advisory; only an audited instructor/admin selection of one finalised, same-team, same-assignment, final-graded, scoreable workflow supplies team usefulness/execution. Individual ownership comes from that learner's own final workflow, prototypes are never averaged, and `SignOff` remains the separate human-recorded company-validation input · Rejected: nomination-driven scoring, member-grade averaging, or selection code that mutates sign-off.
- 2026-07-30 · Provider authorization for versioned assessment grading is frozen in the strict `scoringPolicy.approvedAiProcessors` allowlist and must match the evaluator's immutable `approvedProcessor`; malformed, missing, duplicated, or non-normalized policy values authorize no provider · Non-dataset assessments need the same release-bound processor control as dataset-backed work, without trusting mutable environment routing or arbitrary evaluator JSON alone · Rejected: placeholder provider IDs, environment-only authorization, or reading permissive unparsed scoring JSON.
- 2026-07-30 · Versioned publication previews are content-addressed private staging (`PublicationDecision.previewS3Key`) until an instructor approves the exact current fingerprint; capture/re-capture never updates the review fingerprint itself, and any failed or incomplete fetch marks the staged preview blocked while the last safe public version may remain visible without its stale action · Owner consent and automated safety checks cannot substitute for independent curation of the exact bytes/receipt being published · Rejected: capture-time auto-approval, replacing the public preview before review, or leaving a stale mutable action live after a failed crawl.
- 2026-07-30 · Versioned evidence authorization is an exact receipt join across the frozen public schema, submitted field value, receipt id, receipt fieldKey, and receipt fileRole; dangling, duplicated, wrong-role, or policy-unreferenced receipts remain private even when they share a submission · A submission-level receipt bucket is not a declassification boundary and can contain abandoned uploads or company-sensitive material · Rejected: role-only or newest-receipt selection and exposing every clean receipt attached to a featured submission.
- 2026-07-30 · Legacy compatibility is determined by the submission's immutable `assessmentVersionId` binding, never the assignment's mutable `contractMode`; upgraded assignments may still count their pre-contract graded work toward the matching portfolio slot, but every version-bound consumer remains strict and fail-closed · Editing an assignment cannot retroactively change the contract under which an existing submission was made · Rejected: branching historical scoring/export/publication behavior on the assignment's current mode.
- 2026-07-30 · Supersedes the 2026-07-28 safe-fetch and screenshot SSRF trade-offs: user-controlled HTTP(S) fetches use a request-scoped address-pinned Undici dispatcher with every redirect re-resolved and pinned; screenshot Chromium has direct DNS disabled and receives only GET response bytes through `route.fulfill`, under whole-page request/byte/time budgets, with WebSockets and service workers blocked · DNS validation followed by an independent browser/runtime resolution leaves a rebinding window and does not enforce a complete-page resource budget · Rejected: `route.continue`, cached DNS verdicts, raw browser egress, forwarding learner cookies/auth headers, or accepting partially blocked renders as review-ready previews.
- 2026-07-30 · Every Sessions 3–5 evaluator that sends judgment evidence to a model binds a processor-safe structured anchor pack directly in immutable `AssessmentEvaluatorConfig.anchors`; the pack has its own canonical SHA-256, remains covered by the evaluator/version checksums, supplies exact band criteria and abstract examples to the prompt, and makes the response schema reject wrong bands or triggered-cap violations · Markdown/S3 references alone were not executable grading policy and let a provider ignore authored calibration semantics · Rejected: mutable prompt-only anchors, unverified object pointers, or sending private answer-key examples to the provider.
- 2026-07-30 · Session 5 fixture grading uses three stable pack IDs and one evaluator-only content-addressed bundle; the local comparator binds exactly five ordered cases plus the immutable run-log receipt and persists its checksum-bound `workflowEvaluation` inside the submission's one-to-one `AssessmentResult.structuredFeedback` as the separate version-bound evaluation record · This gives the team rollup an independently queryable exact submission/version/attempt receipt without duplicating lifecycle state, while server code alone owns usefulness 30, execution 20, and ownership 10 · Rejected: free-text pack names, model-scored execution, mutable expected-results lookup, or a second persistence model with a competing completion lifecycle.
- 2026-07-30 · Session 4 final grading binds a content-addressed safe-fetch/render inspection to the immutable assessment/evaluator/submission version, applies only score-lowering deterministic caps, and treats dead, static, uninspectable, privacy-unsafe, or V2-without-reachable-GitHub evidence as review-held rather than fully scoreable · Provider context receives only locally screened checkpoint blocks plus hash/host/count summaries; query/fragment data, repository paths, rendered text, and secrets never enter the provider prompt or persisted inspection artifact · Rejected: trusting learner PASS claims, sending the mutable public page wholesale to the model, or letting a post-provider policy widen/rewrite the grade.
- 2026-07-30 · Authenticated learner assessment drafting, upload reservation, and final commit remain deliberate human-only actions in v1; Forge exposes no autonomous agent submission credential or tool because the click is the learner's academic authorship/consent attestation and resubmission grants bind a named learner/team lane · Instructor operations and evaluator workers keep service interfaces, but an agent may help a learner prepare an artifact only before that learner reviews and submits it in the LMS · Rejected: giving an agent unattended parity for graded submission, consent, appeal, or ownership attestations.
- 2026-07-30 · Selected-row bulk hold resolution accepts at most 200 visible `(holdId, expectedUpdatedAt)` receipts, performs one optimistic `updateManyAndReturn`, and creates all matching audit rows in the same transaction; stale rows remain open, receive no audit, and are returned individually instead of aborting valid selections · This keeps the instructor's select-all path bounded and atomic without hiding concurrent edits · Rejected: one transaction per hold, last-write-wins updates, and all-or-nothing failure on a stale row.
- 2026-07-30 · The hourly retention worker processes one globally oldest deterministic batch (default 100, hard cap 500), filters expired quarantined evidence in SQL before hydration, and bounds exact S3 VersionId resolution; the schema carries the scan/replacement indexes that support this pre-deployment query · This bounds memory, database work, and object-store calls in one job while preserving exact-version deletion and receipt idempotency · Accepted residual: a full oldest batch of permanently held/failing rows can starve later rows until a persisted keyset-continuation design is added; readiness and retention heartbeats must expose that condition · Rejected: an unbounded full-backlog sweep and silent truncation by an over-returning adapter.
- 2026-07-30 · The Session 2 instructor console presents Meme and AI Image work from the active section in a keyboard/click/fullscreen projector overlay; only submitted gallery-backed images enter the sequence, bytes stay behind `/api/gallery/image/:submissionId`, names remain anonymous until that section's reveal, and only the short meme caption is projected · Rejected: opening expiring object-store URLs one-by-one, projecting SCENE prompts, or revealing student names before the existing vote reveal.
- 2026-07-30 · Projector grid boundaries explicitly allow `min-width: 0` and keep the no-wrap thumbnail rail scrolling inside the footer; a development-only browser fixture mounts the real presenter with 48 images to guard the production wiring · Rejected: a synthetic DOM copy that could pass after the real component lost a containment style.
- 2026-07-30 · Emergency Section F enrollment admits only Clerk-authenticated accounts with no LMS user match, is controlled by an absolute UTC expiry with a hard 30-minute maximum, and creates student-only Section F rows; concurrent conflicts are claimed only when the existing row is already a Section F student with no conflicting Clerk identity, so existing roles and sections are immutable · Restores class access without creating an anonymous/test-login bypass or leaving an open-ended enrollment mode · Rejected: disabling the roster gate, trusting client metadata, or upserting over existing users.
- 2026-07-30 · Section F keeps one canonical LMS User while `UserEmailAlias` accepts both personal and institution addresses and `UserClerkIdentity` maps multiple Clerk accounts to that User; the legacy `User.clerkUserId` remains a first-account fast path, all writes also populate the identity table, cross-namespace email triggers enforce one owner, and DPDP erasure flags every linked Clerk identity · Preserves submissions and grades while making either Google account usable without a per-request Clerk lookup · Rejected: duplicate LMS users, overwriting one Clerk id back and forth, or replacing every personal email with the institution address.
- 2026-07-31 · Legacy Session 2 image projection prefers the committed `Submission.files[0]` S3 key and uses `fields.image` only as an old-row fallback · The reservation-backed uploader now stores a reservation id in `fields.image`, while older rows stored the S3 key in both places; signing the reservation id produced valid-looking redirects to nonexistent objects · Rejected: treating every non-empty legacy image field as an object-store key.
- 2026-07-31 · Emergency first-sign-in enrollment is configured by an explicit single-letter section code plus a near-term absolute expiry, with the previous Section F variable retained only as a compatibility fallback · Section A needed the same controlled recovery path without duplicating authentication logic or overwriting existing users, roles, and sections · Rejected: hard-coding another section, disabling the roster gate, or moving existing users during sign-in.
- 2026-08-03 · The Session 3 Data Race is reissued as seven items for sections B, C, D, E and G only; A, F and H keep the released ten-item pack because their learners already answered it, so a release now carries just the sections being issued and the validator checks section membership rather than requiring all of A-H · Items stay bound to the frozen 30 Jul 2026 snapshot, whose underlying cell values were verified identical to the learner-facing Google Sheet, and each item states its own filter, blank rule and rounding so exactly one option is defensible; the three distractors are the outputs of named wrong methods — contains instead of exact match, blanks folded into zeroes, `category` instead of `markets_json`, stated 0.0x multiples discarded, mean-of-ratios instead of ratio-of-sums, zero-MRR rows dropped, and mean reported as median · Answers produced without inspecting the data land on a distractor, so the item discriminates on reading the data rather than on typing speed · Rejected: regenerating a section that has already raced, money distractors within 2% of the answer that read as rounding artifacts, and market tags that are substrings of other tags, where a wildcard search and a list-membership test would disagree.
- 2026-08-04 · Learners may replace their own submission while the assignment gate is open, controlled by the new additive `AssignmentType.allowSelfReplace` flag rather than a hardcoded slug, and enabled for `presentation` only · Replacing writes the next version and supersedes the previous one everywhere reads already take the latest, so grades, gallery items and publication decisions keep pointing at the version that produced them and the one-submission-per-student rule still holds for every other type · Rejected: destroying the prior submission row or its S3 object, which would break the immutable-version invariant and orphan downstream records, and gating the behaviour on a code list, which would make a new artifact kind need a deploy.
- 2026-08-04 · AI grading is switched off for every assignment type by clearing `AssignmentType.aiGraded`, which stops the Anthropic enqueue without a deploy and moves gallery publishing for the gallery-eligible types from post-grading to submit time, matching how `meme` already behaves · Chosen over an environment kill-switch, which would have left `ai-image`, `app` and `workflow` never reaching their galleries because the grading worker is what syncs them today · Reversible per type by setting the flag back.
- 2026-08-04 · The learner's own submission view resolves a file field through `SubmissionEvidence` regardless of contract mode · The uploader writes an evidence id into the field for legacy rows too, but the read path resolved it only when `assessmentVersionId` was set, so every legacy row signed the raw id and produced a valid-looking presigned URL to a nonexistent object — learners could not reopen their own upload, verified as 404 on five production rows and 200 after the fix · Resolution is a pure exported function with unit tests covering cross-field, unscanned-evidence and raw-key-passthrough cases, since the surrounding read path is DB-backed and skips without a database.
- 2026-08-26 · Session 8 bridges the previously taught voice-agent session into one RAG + MCP system: the existing local RAG simulator supplies evidence, while a connection-free Make scenario exposes one draft-only lead tool through an instructor MCP client; students can complete the core exercise without a paid MCP client, and any human approval remains a separate, non-agent-controlled flow · Keeps the class feasible across plans while teaching least privilege and testable tool contracts before real writes · Rejected: a production CRM/email action in class, an agent-supplied approval boolean, and a class that depends on every learner connecting a paid MCP client.
- 2026-08-26 · Session 8 uses Kōdō Academy’s SupportFlow module as an LMS-native deterministic retrieval lab before the existing PraxelPay browser-embedding challenge: the first makes chunk boundaries, Top-K, keyword/concept blending, ranked passages and A/B configuration decisions observable; the second demonstrates real semantic retrieval against conflicting and hostile evidence · Keeps the owned case useful without coupling the live class to Kōdō’s separate Python/Postgres stack or misrepresenting classroom scores as production embedding similarity · Rejected: merely linking the private academy repo, importing its full deployment, or replacing the real-embedding challenge with lexical simulation.
- 2026-08-31 · Lovable app peer review is a separate, section-gated round: every rostered student receives five stable distinct other authors/apps, gives three 1–5 scores and a 20-word minimum comment, and sees explicit 1/3/5 anchors. Imported app URLs and completed reviews are immutable; faculty can replace a reported unfinished target while preserving its report. Student payloads exclude creator identities and private briefs. The grade-receipt requirement is instructional only; no grade weight, automated grade restriction, teammate PCI change, or original-submission mutation is applied. Source CSV validation is all-or-nothing against canonical emails/verified aliases; duplicate submissions require deliberate selection, not “latest wins.” See `operations/app-peer-reviews.md` for activation and verification.
- 2026-09-03 · Interview v2 replaces the Session-8 viva in place rather than running alongside it as a second `Interview.kind` · One table, one flow and one grade; two rubrics and two windows would have to be maintained forever · Rejected: an `Interview.kind` discriminator.
- 2026-09-03 · Sarvam voice ships through the first-party `livekit-agents[sarvam]` plugin (`sarvam.STTRealtime`, falling back to `sarvam.STT` when the installed plugin predates it, plus `sarvam.TTS`), selected as a complete PAIR by `SARVAM_API_KEY` with Deepgram+ElevenLabs retained as the no-key fallback · The official plugin already streams both halves with documented AgentSession wiring, and pair-selection keeps local dev and a Sarvam outage running · Rejected: hand-written STT/TTS adapters against Sarvam's REST API, and hard-requiring all four voice keys.
- 2026-09-03 · Sarvam STT defaults to `auto` language identification rather than a pinned `en-IN` · The cohort code-mixes English and Hindi mid-answer; pinning drops or mangles those spans, which reads to the grader as incoherence and penalises exactly what the rubric forbids penalising · Rejected: pinning `en-IN`.
- 2026-09-07 · Student interviews have one graded transport: LiveKit realtime. A browser disconnect remints a token for the same room, the agent keeps its session for a bounded rejoin grace period, and expiry reports recording only rather than grading a partial transcript. Sarvam remains the primary speech pair; Deepgram plus ElevenLabs is an emergency provider failover, not a student-facing recorder · The screenshot showed that manual recording both hid a missing provider and cut off the real interview · Rejected: converting a failed realtime connection into typed or uploaded answers.
- 2026-09-03 · The LiveKit join-token TTL moved to 25 minutes when the interview budget rose to 15 · At the previous 15m parity a student dropping near the end got an expired token and degraded to turn-based on the clock rather than the network; the room outlives the token by design, so widening costs nothing · Rejected: leaving the TTL at the interview budget.
- 2026-09-03 · The interview scores two axes (`conceptual_understanding`, `work_integrity`) at 0–50 each, and `lib/scoring/components.aiInterview` reads whichever rubric shape a row carries · Both shapes total 100, so the course weighting is untouched, and no backfill ever rewrites historical grade evidence; the CSV export keeps columns for both shapes · Rejected: migrating old rows to the new keys, and dropping the legacy export columns.
- 2026-09-03 · Both interviewer and grader prompts are told that grammar, accent, vocabulary, hesitation and code-mixed English carry no score effect, and the grader is told not to reward confident delivery · The cohort speaks English as a second or third language into a machine transcriber; without an explicit rule the model scores polish · Rejected: leaving fairness implicit in the rubric bands.
- 2026-09-03 · `InterviewPrerequisite` is its own table and its objects are presigned directly rather than through `GeneratedObjectReservation` · That machinery binds an object to exactly one parent row (submission or interview) and a prerequisite has neither — it is uploaded before any interview exists and survives attempts; `Submission` would also give a resume assignment, grade and gallery semantics it must never have · Rejected: widening the reservation parent invariant, and modelling prerequisites as submissions.
- 2026-09-03 · DPDP erasure collects interview video and prerequisite objects explicitly · Prerequisites are user-scoped so nothing in the submission or interview graph reaches them; a resume is PII and would otherwise survive erasure in S3 with only its row deleted · Rejected: relying on the generic reservation sweep.
- 2026-09-03 · Platform-observed flags live in `Interview.systemFlags`, separate from the grader's own flags inside `rubricScores` · `video-lost` is observable only by the client; the grading model cannot see a track end and must never be able to emit or clear one · Rejected: adding `video-lost` to the model-emittable `INTERVIEW_FLAGS`.
- 2026-09-03 · Interview v2 rollout is the `ConfigKV` row `interview_v2` (`{open:boolean}`, absent = closed) and the closed check runs BEFORE the prerequisite and window guards · `GateTarget` has no interview member and the interview is scheduled by `InterviewWindow`; adding an enum member would touch every gate-resolution path for one boolean, and a student who cannot enter should not be told they are missing an upload · Rejected: a new `GateTarget` value.
- 2026-09-03 · Escalation for a dropped call and the instructor's reply are composed text (mailto + copyable draft), carrying progress and never a score · No transactional email exists in the repo, and a provider plus secret plus delivery log is not warranted at this volume; grades reach students through instructor review, not a support mail · Rejected: adding Resend.
- 2026-09-03 · Interview video retention rides DPDP erasure and the generic uncommitted-reservation sweep; NO `RetentionPolicy` class was added · `RetentionPolicy` is only ever reached through `AssessmentVersion` in this codebase, so an interview-video class would be configuration nothing reads — dead config that looks like compliance is worse than none · Rejected: provisioning an unread `interview-video` retention class.
- 2026-09-03 · Resume uploads accept PDF and plain text only; DOCX is refused with the fix named · `lib/ai/extract` reads PDF and text, and a DOCX parser is not worth a dependency for a format students can export straight past · Rejected: adding a DOCX parser, and a generic unsupported-type error.
- 2026-09-03 (superseded same day) · The interview dialog LLM was briefly Claude via the `anthropic` plugin · `ANTHROPIC_API_KEY` already exists on the web and worker services so no new credential is introduced; the interviewer ends the session by calling the `end_interview` tool, so reliable tool calling is load-bearing, and the dialog model reads student-uploaded material, so injection resistance matters as much as fluency — the grader is already Claude, so interviewer and grader now share a family · Rejected: Gemini Flash (a third provider and a GOOGLE_API_KEY alias for no gain) and Sarvam-M via its OpenAI-compatible endpoint (the livekit sarvam plugin ships STT and TTS only, and unproven tool-calling is a poor bet inside a graded assessment).
- 2026-09-03 · The interview dialog LLM is `google/gemini-3.6-flash` served through **LiveKit Inference**, not a direct provider plugin · Inference is included in LiveKit Cloud and authenticates with the `LIVEKIT_*` credentials the worker already needs, so the agent now carries NO LLM key at all; it is zero-data-retention by default, which matters because the prompt carries the student's resume; and at ~$0.0014/min it costs ~$2.50 for a 120-student cohort against the plan's $5 monthly inference credit, versus roughly $66 on Claude Sonnet, at materially better time-to-first-token · Chosen over `google/gemma-4-31b-it` (lower latency but a 31B open-weight, and the agent must reliably CALL `end_interview` while holding student-supplied text engineered to move a grade — the latency gap is small beside the STT/TTS round trip, the instruction-following gap is not) · Rejected: Claude Sonnet (cost and TTFT wrong for a realtime voice loop), Gemini via a direct plugin (a key and a bill for what Inference already covers), and Sarvam-M for dialog (its livekit plugin is STT/TTS only). Sarvam keeps STT and TTS, where Indian-language and code-mixed speech is the whole point and LiveKit's own STT options are weaker.
- 2026-09-09 · A re-dispatched agent RESUMES the interview: it seeds its `ChatContext`, `question_count` and own-work coverage from the transcript `agent-context` already returned, and greets only on a genuine first join · A job is not an interview — the room name is deterministic, so every job that ends while the interview stays live is followed by a new job for the same interview, and an empty `AgentSession` meant greeting the student and restarting at segment one; one student was restarted four times inside nine minutes and lost his interview, and his transcript carries four greetings. The payload to fix it had been in the route since it was written and nothing read it · Rejected: re-asking from the top and letting the grader sort it out.
- 2026-09-09 · `/api/interview/token` deletes a resumed interview's room when it has no interviewer and is past a cold-start grace, and the agent deletes its own room on every aborted exit · LiveKit automatic dispatch fires once, at room creation, so a room whose agent left will never be given another one; the student reconnects to it indefinitely, watching themselves, while the 5s transcript poll keeps `lastSeenAt` fresh enough that the abandonment sweep cannot see them either. Deleting the room makes the next join create one, which dispatches · Rejected: explicit dispatch via `agent_name` + `AgentDispatchClient` — correct, but it makes automatic dispatch stop the moment the worker deploys, so any web build that is not yet dispatching admits students to rooms no agent ever joins; that is a flag-day on a live cohort.
- 2026-09-09 · `/api/interview/agent-complete` completes the interview BEFORE committing the recording, and a failed commit is reported rather than thrown · The agent posts the Egress key the moment `stop()` returns, which is while the MP4 is still uploading; committing it HEADs an object S3 does not have yet, threw, and the completion underneath never ran. The student heard "your interview is complete" while the row sat live forever — never graded, never swept, `costUsd` stuck at 0 because grading is the only writer of it · Rejected: waiting for `EGRESS_COMPLETE` before completing, which puts an upload on the critical path of a student's grade.
- 2026-09-09 · The student's interview clock counts from `Interview.createdAt`, served by the token route, not from room-connect · A reconnect reset the displayed timer to 00:00, so a student four questions in read the blip as his interview starting over. The agent gained a matching wall-clock ceiling from the same timestamp, since the per-session budget restarts on every rejoin and could otherwise be extended indefinitely · Rejected: leaving the clock per-connection and explaining it in copy.
- 2026-09-09 · The abandonment sweep auto-grants a retake when the interview died within the first two interviewer turns, and says so in the escalation reason · A greeting plus at most one question means the student never got an interview to walk out of; all four students lost to the dialog outage sit at 0–2 agent turns and every one needed a grant issued by hand two days later. Above that line a silent room is a judgement call and stays with the instructor · Rejected: auto-granting on any abandonment, which makes bailing out a self-service retry.
- 2026-09-09 · `startInterview` takes a per-student advisory lock and re-checks for a live interview inside its transaction · The token route's live check is a read, and two of its POSTs can race — the 10s retry poll against a manual retry, or two tabs — leaving two live rows, two rooms, two agents and two of the thirty slots, with the orphan escalated 30 minutes later and able to outrank the real interview in the lobby · Rejected: a partial unique index, which Prisma cannot express in schema and would drift against every future `migrate diff`.
- 2026-09-09 · The interview never completes with zero student turns, whichever clock expires · A student who dropped during startup — before the participant handlers were even registered — had the agent greet an empty room, question it for twenty minutes, and then complete a transcript containing no answers, which grades silence and spends their single attempt · Rejected: treating budget expiry as a normal finish.
- 2026-09-09 · `POST /api/interview/complete` is retired (410), like `/start`, `/answer` and `/fallback` · It let any student end and grade their own interview mid-conversation; no client calls it, but it was owner-scoped and reachable, and the agent's later turns would 409 and be dropped · Rejected: leaving an unused endpoint that only the honest do not call.
- 2026-09-09 · A resumed agent job keeps the first job's recording instead of starting a second egress · The video reservation is idempotent per interview, so a re-dispatched job is handed the same S3 key and its egress writes over the first one's file — and now that a re-dispatch resumes rather than restarts, that path is ordinary rather than exotic. The transcript, which is what grading reads, is complete either way · Rejected: per-job reservation ids, which would mean one interview owning several recordings and a commit path that has to choose between them.
- 2026-09-09 · The prerequisites card states why the start control is absent instead of saying "you can begin your interview below" unconditionally · Every section's window had closed the previous night, so a student who had uploaded all three artifacts was shown that line with nothing underneath it and no way to tell whether he was early, late, locked out, or looking at a broken page · Rejected: hiding the card once uploads are complete, which loses the receipt that they were done.
- 2026-09-09 · `resolve` refuses `mark-graded` on an interview with no `rubricScores` · A swept-abandoned interview is scoreless and sits in the same instructor list as an integrity flag, next to a button that reads like the way to clear it; flipping it to `graded` leaves the student on "your score appears here in a minute" forever while the assembler counts the component as complete and worth nothing · Rejected: letting the instructor decide, when the failure is silent and lands on the student.
- 2026-09-09 · One unused `InterviewRetake` per student is enforced by a partial unique index, not by the read-then-create in each caller · Both writers check then act — the instructor route and the sweep's auto-grant — so two concurrent sweeps, or a sweep racing an instructor, could each read "none" and each insert, handing a student two extra attempts at a graded assessment. Partial so that USED grants, which are the record of every retake ever issued, still accumulate freely · Rejected: serializing the callers, which does not help when the two are different processes.
- 2026-09-09 · Turns restored into a resumed agent's `ChatContext` carry an `lms-restored-` id and the turn handler drops anything wearing it · livekit-agents 1.7.1 emits `conversation_item_added` only from live speech and generation, never for a `chat_ctx` supplied at construction — so this is unreachable today. It is here because "the SDK does not currently do that" is not a guarantee worth a student's transcript, and the failure would be silent: a duplicated transcript, an inflated question_count and a runaway guard tripping mid-interview · Rejected: content-based dedupe, which would eat the legitimate repeats — students ask the interviewer to repeat a question all the time.
- 2026-09-09 · The agent waits for the egress to report EGRESS_COMPLETE before reporting the S3 key, and completes the interview in a separate, earlier post · stop_egress returns while the MP4 is still uploading, so the key was always reported before the object existed and the commit's HEAD failed on every single interview. Completion goes first and alone, because an S3 write must never sit on the critical path of a student's grade · Rejected: committing without verifying the object, which is the reservation invariant the whole generated-object layer exists to hold.
- 2026-09-09 · The interview-video reservation gets a 3-hour TTL instead of the 30-minute default · It is reserved when the interview STARTS, and an interview runs 20 minutes on its own clock and up to an hour across reconnects — so the reservation could expire while the student was still talking, and every later attempt to attach the recording returned "no longer active" · Rejected: raising the global reservation TTL, which is 30 minutes for good reasons everywhere else.
- 2026-09-09 · The sweep attaches recordings that were never committed, and the instructor page finally plays them · `Interview.videoS3Key` was written by agent-complete and read by nothing at all — nine interviews had a recording in S3 that no instructor could reach, which is the same as having none · Rejected: leaving the backstop to a manual script, given the attach had silently failed 100% of the time and nobody noticed for six days.
- 2026-09-09 · The temporary-enrollment ceiling moves from 30 minutes to 7 days · At 30 minutes this is an emergency hatch someone stands beside and resets; the actual problem is 55 students on the roster under a personal address who hit "Not on the roster" the moment they sign in with their institutional one, across a six-day interview window. Still a ceiling and not a switch — a deadline further out is ignored, so it cannot be left open by forgetting · Rejected: auto-provisioning any authenticated Google account, which turns a graded assessment into open enrollment.
- 2026-09-09 · Interview scores are exported through one builder (`lib/interview/score-export`) behind two routes: the instructor download and a token-authenticated feed · A spreadsheet that refreshes itself on a timer cannot hold a Clerk session, and a score that reads one way in a download and another way in the sheet is worse than either alone · Rejected: a second query for the feed, and a public unauthenticated CSV — the feed carries every student's score, so it is 503 unless `INTERVIEW_EXPORT_TOKEN` is set and revoking it is one env-var deletion.
- 2026-09-10 · An assignment carries two deadlines: `Assignment.dueAt` is the hard cutoff every backend rule already reads (grade finalisation, cohort freeze), and the new `Assignment.displayDueAt` is the earlier soft date learners are shown, resolved in one place by `lib/deadlines` · Publishing a date ahead of the real cutoff is the only way to get work in early without either lying in a brief string or moving the cutoff the finalisation rules depend on; keeping the soft date out of every backend path means a grading rule can never accidentally enforce a deadline the course chose not to enforce · A DB CHECK holds `displayDueAt <= dueAt` so a shown date can never advertise a window finalisation would refuse, staff hub views render the hard cutoff beside the shown one so an instructor is never told "due the 10th" by the same app that will not finalise until the 15th, and student payloads carry the shown date alone · Rejected: a global soft/hard offset in config (deadlines differ per assignment), and overloading the gate's `closedAt` as the displayed date (gates are availability, not promises).
- 2026-09-10 · A prerequisite with no extractable text is read by LOOKING at it: `worker/jobs/prepare-prerequisite` falls back to a vision transcription (image block, or PDF document block for a scan) before giving up, and the existing digest step then runs on that transcript unchanged · A sector map is a DRAWING — students build it in Figma or Canva and export a PNG — so the text path was always going to return nothing, and eleven students sat the interview with the interviewer blind to the one artifact the `own_work_defence` segment is built on · Transcription and digest stay separate steps so the pipeline keeps its shape (extract → digest → prompt) and the interviewer prompt needed no change · Rejected: server-side rasterisation plus OCR (loses layout, which is most of a map's meaning), and treating an image prerequisite as simply absent.
- 2026-09-10 · `extractJsonObject` repairs unescaped control characters inside string literals before giving up, and only after a plain parse has already failed · A vision transcription is long prose, and models routinely put a real newline inside the JSON string; two students' maps were lost to `Bad control character in string literal` on a response that was otherwise perfect · The repair runs only on the failure path, so a well-formed response parses byte-identically and a genuinely malformed one still throws · Rejected: asking the model to escape its own newlines (unreliable at length) and switching the field to an array of lines (changes every caller's schema).
- 2026-09-10 · The interview asks in plain language and the grader scores the thinking, not the vocabulary: the regulated-shipping probe no longer treats naming HIPAA/SOC 2/PCI-DSS as the mark of a strong answer, `rag_mcp` asks "how would you know if it was getting things wrong" rather than naming retrieval, and `own_work_defence` accepts a business account of a trigger as complete · The cohort runs businesses; a student who reasons correctly about who gets hurt and who is liable has answered the consequences question, and scoring them below someone who knew the acronym measures vocabulary rather than judgement · Interviewer and grader were changed together so the two cannot drift — a question asked plainly but marked against a technical checklist is worse than either alone · Rejected: softening the grader alone (the interview would still feel like a systems-design screen) and dropping the probes entirely.
- 2026-09-10 · The interviewer requires a few words and a moment of speech before a student can interrupt it (`INTERVIEW_MIN_INTERRUPTION_WORDS`, default 3; `INTERVIEW_MIN_INTERRUPTION_SECONDS`, default 0.6), passed to `AgentSession` by signature check so an older wheel degrades rather than failing to start · Sarvam runs `language=auto` for code-mixing and invents filler on silence — one transcript carries seventeen turns of the bare word "I", seven of "I mean," and a line of Bengali the student never spoke — and LiveKit's default lets ONE transcribed word barge in, so every phantom fragment killed the question mid-sentence; that interview holds four truncated openings of the same question, and the student reported it as the interviewer freezing and hallucinating through a silent call · 66% of his student turns were under eight characters, and 128 agent questions were cut off across the wider cohort, so this was degrading every interview to some degree, not one student's microphone · Rejected: pinning Sarvam to en-IN (drops the code-mixing the cohort actually speaks, already rejected once), and filtering short turns out of the transcript, which hides the symptom while the interviewer still gets cut off.
- 2026-09-10 · A question the student talked over and the interviewer restarted counts ONCE against the question budget (`is_restart_of`, prefix match on normalised text) · Every fragment of an interrupted question arrives as its own conversation item, so barge-in inflated the count: one interview logged 26 agent turns for about 12 real questions, the model believed it was past the ~20 it is told it has, and closed with 5m57s of a 20-minute budget unused — after the sector-map segment had received exactly one answer · Prefix, because that is precisely what an interruption leaves behind ("In" → "In your sector map, you argue that the compute moat is" → the whole question); a question deliberately asked again in full is not a prefix of anything and still counts · Rejected: dropping the fragments from the transcript (hides the evidence of a platform fault from the instructor reviewing it) and raising the budget (treats the symptom and lengthens every healthy interview).
- 2026-09-10 · A deploy no longer cuts live interviews short: `RAILWAY_DEPLOYMENT_DRAINING_SECONDS=1500` on forge-agent, and `WorkerOptions(drain_timeout=…)` to match (signature-guarded, env-tunable) · Railway gives a torn-down deployment ZERO seconds before SIGKILL by default, so every push killed the worker while it held live sessions — students lost their interviewer and waited ~90s for the room to be re-dispatched, and the whole "wait for a gap before deploying" dance existed only to work around it · Both halves are required: the platform has to allow the window and the worker has to drain inside it rather than exit on the signal, and the code value must stay at or below the service value or Railway kills the worker mid-drain · Rejected: keeping the deploy-into-a-gap workaround, which does not scale past one cohort and fails outright once interviews run continuously.
- 2026-09-14 · The interview grader sees consecutive student turns joined into one answer (`joinStudentRuns`), and its prompt says a reply that continues an answer the interviewer talked over belongs to the earlier question and is never scored down or called "fragmented" · Speech-to-text ends a student turn at every pause and the interviewer's endpointing jumps in mid-answer, so 313 of 347 graded interviews carry split answers (median 13 per interview); a student reported his continuation being marked against the next question, and the grader's own rationale to him cited "fragmented" answers — a platform fault scored as a student weakness · Only the grader's view is joined: the stored transcript keeps every row so an instructor sees what actually happened, and interviews already graded are re-marked with `pnpm interview:remark`, which never lowers a mark · Rejected: rejoining answers across an interviewer turn in code (which question a continuation belongs to is a judgement the grader is better placed to make), and per-student retakes (the same endpointing would split the retake too).
- 2026-09-14 · Course 2 ("the Shipyard") is built as a `courseId`-scoped mode of the Forge — `Shipyard*` tables each carrying `courseId String @default("course-2")` and a `SHIPYARD_COURSE_ID` constant — rather than a `Course` table or a second repository · Auth, roster, S3 presign, SSRF guard, pg-boss, AuditLog, CostLog and the brand are already solved here, and the Forge's schema notes already said its tables were designed to gain a `courseId`; a real Course table is a migration away and nothing in the Shipyard assumes exactly one course · Rejected: a second Next.js app (a second Clerk instance, a second S3 policy, and two copies of every invariant), and adding `courseId` to the Forge's own `Submission`/`Grade`/`Review` tables, which would have made every Course 1 query a two-course query for no Course 2 benefit.
- 2026-09-14 · The Shipyard is served from the existing web service at `lms.praxel.in/shipyard`, not a `shipyard.praxel.in` of its own · Clerk's production instance is bound to `lms.praxel.in`, so "the same login as Course 1" and "a separate domain" cannot both be true without a Clerk satellite domain and new DNS; a path prefix gets the whole cohort in on day one · Rejected: a satellite domain before M0 is even clickable, and a separate Clerk instance, which would mean students signing in twice for one programme.
- 2026-09-14 · Shipyard reviews run on their own Railway service (`shipyard-worker`, `railway.shipyard-worker.json`) from the same image as the Forge worker · Reviewer concurrency is 15 and a deadline night is 480 near-simultaneous submissions; sharing a worker would make Course 2's burst decide how fast Course 1's interviews get graded, and Playwright renders belong in exactly one process · Rejected: one worker with per-queue concurrency, which shares a memory ceiling and a deploy with the course it is not serving.
- 2026-09-14 · The Shipyard migration was authored with `prisma migrate diff --from-schema-datamodel` against `git show HEAD:./prisma/schema.prisma`, not `prisma migrate dev` · `migrate dev` replays every migration into a shadow database, and the July roster migration (`20260730143000_section_f_email_aliases_and_fg_rosters`) is data-dependent and fails there, so `migrate dev` has been unusable in this repo for months; `migrate diff` produces the same SQL from the two schemas and `migrate deploy` applies it · The generated SQL is reviewed by hand before it is applied, and migrations stay forward-only · Rejected: repairing the July migration (forward-only means never editing an applied one) and hand-writing the SQL, which drifts from the schema silently.
- 2026-09-14 · `ShipyardTrackerOverride` exists for demos and `TRACKER_MODE=fake` only, and `setFakeSignals` throws when `TRACKER_MODE=real` · The whole metric-gate story — payments going live, the tenth workflow run landing at midnight, a self-payment flag failing Launch — has to be demonstrable and testable with no live tracker, but a table an admin can write that clears a gate would make "a metric gate is never cleared by anything typed inside this portal" false · Rejected: a seed-only fixture with no table (the demo needs to flip a signal live, in front of a room) and an env-var-free fake, which would have made the refusal impossible to enforce.
- 2026-09-14 · Checkpoint 1 is the one gate cleared by evidence the student reports themselves — a waitlist URL, a signup-count screenshot and the traffic-test numbers · Shipped.money has no waitlist-signups source, and demand before anything is built is the checkpoint the course most needs; the bar says plainly that this one is on trust, low-confidence and outlier reviews go to a human rather than passing quietly, and the screenshot must show the count inside its own tool with a date · If gaming appears, a waitlist source moves to Shipped.money in v1.1 · Rejected: dropping the demand evidence (the idea checkpoint becomes an essay) and building a waitlist tracker here, which puts a second source of truth for real numbers inside the portal.
- 2026-09-14 · The grade's raw components are derived by one documented, monotonic formula in `lib/shipyard/scoring.ts`: productQuality is the mean of the checkpoint 3 and launch reviewer scores (a missing score is skipped, not zeroed); realNumbers is half paying customers and half gross payments, both on a `log1p` curve saturating at 10 customers and $100 verified; workflow is piecewise linear — 0 at no runs, 80 at the bar of ten, 100 at thirty; distribution is 70% the launch write-up's distribution criteria plus 30% customer reach, and spam zeroes it outright · Log curves because the first paying customer is the hard one and the hundredth is not, and the course should not pay a student more for scale than for proof; monotonic in every input so no student is ever better off with fewer customers, fewer runs or a worse review; a missing review is skipped so a student mid-course is not punished for not having reached the launch review · Rejected: linear revenue (one student with an outlier month would dominate the cohort) and counting an unreached checkpoint as zero.
- 2026-09-14 · The OpenRouter slugs and prices are pinned in `lib/ai/router.ts` as confirmed today: `anthropic/claude-haiku-4.5` at $1.00/$5.00 per million and `z-ai/glm-5.3-flash` at $0.15/$0.50 per million, with `computeCostUsd` as a pure function beside them · Every review has to record what it cost, and a price table scattered across call sites is a cost meter that quietly lies; the provider-reported `usage.cost` is preferred when OpenRouter sends one, because it already accounts for caching and promotional rates the table does not know · GLM's current 50% promotion is deliberately NOT in the table — budget at the base rate · Rejected: reading prices from OpenRouter's models endpoint at call time (a second network dependency on the critical path of every review).
- 2026-09-14 · The BYOK kill-switch flips after FIVE consecutive credit-or-auth failures, and only those: a 500, a 429 or a timeout never advances the counter · Five is enough that a transient auth blip does not move the whole cohort onto Flash, and few enough that a genuinely exhausted Anthropic credit costs at most five failed-call latencies rather than one on every review for the rest of the night; once flipped, `anthropicExhausted` stays set until an admin resets it, and the profile change writes an AuditLog row (`shipyard.router.flip`) carrying both sides · Rejected: flipping on the first failure (one bad minute at the provider would silently change which model grades a cohort) and a time-window counter, which is harder to reason about and to test than a run.
- 2026-09-14 · The tracker runs in fake mode until Shipped.money ships `GET /api/v1/checkpoint-signals?projectId=<slug>`; the contract we depend on is pinned in `lib/tracker/real.ts` and tested there · Shipped.money is live (`https://web-production-f46c6.up.railway.app`) with cohorts, project pages at `/p/<slug>` and anomaly flags including a self-purchase check, but it has no checkpoint-signals endpoint and no public JSON API yet, so M3 would otherwise have nothing to call · The contract: bearer `SHIPPED_MONEY_SERVICE_TOKEN`, `X-Shipyard-Timestamp` in unix seconds, `X-Shipyard-Signature` a lowercase-hex HMAC-SHA256 keyed with that token over `${timestamp}:${projectId}`; 200 returns the SPEC §8.5 shape plus optional `workflowRuns`, with money in USD minor units; 404 means the project is not on the tracker and 401/403 means our credentials are wrong, and all three return null rather than throwing · `ShipyardProduct.trackerProductId` stores the project SLUG, which is what a student pastes · Rejected: scraping the project pages, and a token-only request, which a leaked bearer would let anyone replay.
- 2026-09-14 · A tracker blocking flag makes EVERY metric signal false, not just the launch gate's · A self-payment invalidates the payment evidence wherever it is read, so a student flagged at launch also stops clearing the money gate; the student's spine shows `blocked-by-flag` rather than a bare "awaiting metrics", so the reason is never a mystery · The consequence is visible in the seed: the two flagged students sit at money, not at launch · Rejected: failing only the checkpoint whose required signals name `noBlockingFlags`, which would let a gamed payment keep a gate it had already cleared.
- 2026-09-14 · The Shipyard UI uses the Shipped.money visual language (white canvas, #f7f9f8 workspace, forest green #175b44 actions, lime #d5ef83 accent, Instrument Sans/Serif, IBM Plex Mono for numbers, 10/16px radius) instead of the Praxel brand, scoped to the `.sy-root` subtree so the Forge is pixel-identical · The client overrode SPEC §0 mid-build ("You don't need to stick with Praxel brand. Please override that with shipped.money brand for UI"); the tracker and the portal now read as one product to a student moving between them · Rejected: keeping Praxel tokens with Shipped type only (two half-brands), and a global theme switch (the Forge must not move).
- 2026-09-14 · A student's queue position is computed COURSE-WIDE in `lib/shipyard/spine.ts` (`1 +` the count of `submitted|in_review` submissions with an earlier `submittedAt`), not per section or per checkpoint · The reviewer is one pool of `SHIPYARD_REVIEW_CONCURRENCY` workers draining one `shipyard.review` queue, so "you are 4th" only predicts a wait if it counts the same queue the workers pull from; a per-section number would read lower and mean nothing on a deadline night · It is computed in the loader rather than stored on the row because it is true only for an instant, and a stored position would be wrong the moment the job ahead of it finished · Rejected: a position stored at enqueue time (stale within seconds) and reading pg-boss's own queue depth from the web tier, which couples the student's page to the job table's internals.
- 2026-09-14 · Every student-supplied URL is liveness-checked through `lib/net/safe-fetch` at SUBMIT time, in `createSubmission`, as well as later in the worker · A dead waitlist page is the most common reason a checkpoint 1 attempt comes back, and a student who learns that ninety seconds later has spent an attempt and a fifteen-minute cooldown on a typo they could have fixed while the form was still open · The refusal names the field, and a private or loopback address is refused as "not reachable from here" without a connection ever being made; the worker still re-checks, because a page can go down between submitting and being reviewed · Rejected: checking only in the worker (cheap for us, expensive for the student) and checking only in the browser, which is not a check at all.
- 2026-09-14 · `shipyard.review.dead` HAS a consumer, unlike the Forge's `grade.submission.dead`: it returns the submission with the single reason "The reviewer could not run. Resubmit, or ask your instructor." and flags the review for a human · A submission stuck in `in_review` is not merely ungraded — the resubmit form stays shut while an attempt is in review, so a reviewer outage silently removes the student's only way forward and they watch a queue position that never moves. A return is recoverable: the cooldown lifts, they resubmit, it works · The Forge can leave its dead letters queued for an admin redrive because no Course 1 gate is waiting on one; here the gate IS the student's next step · Rejected: a sweep that re-enqueues dead letters (the four retries already failed) and leaving them for the admin view, which is the behaviour that strands the student.
- 2026-09-14 · Shipyard file keys are scoped by PRODUCT (`shipyard/<productId>/<checkpointKey>/<id>-<filename>`, minted only by `keyForShipyardUpload` from the session) and a submitted key is verified by prefix alone · The key is derived server-side from the session's product and never from the request body, so the prefix is not a hint about ownership — it is the same fact the database holds, carried in the key; that makes the check on submit one string comparison rather than a lookup per file on the critical path · Product rather than user because everything in Course 2 hangs off the product, and a `courseId`-scoped product id survives a student changing their login · Rejected: an UploadReservation table like the Forge's (the Shipyard has no versioned assessment contract to bind to) and trusting the client's key, which would let one student attach another's screenshot to their own submission.
- 2026-09-14 · M1's stub reviewer returns an attempt when any field value contains the literal `[review:return]`, and that token is a demo affordance only · The pipeline — submit, queue, verdict, gate flip, notification, next checkpoint open — has to be demonstrable end to end before a model is involved, and a demo needs a reviewer that fails on command as well as one that passes; the token lives in `worker/shipyard-jobs/review-submission.ts` and nothing outside that file knows it exists, so M2's real reviewer simply never reads it · The same completion path (`lib/shipyard/review-complete.ts`) serves the stub, the instructor's `POST /api/shipyard/admin/review-stub`, the dead-letter backstop and M2's model call, so "a verdict was recorded" always means the same five things happened · Rejected: an env-var demo switch (one more thing to be wrong in production) and seeding pre-written verdicts, which would not exercise the queue.
- 2026-09-14 · `image/heic` and `text/markdown` were added to the Forge's `UPLOAD_TYPE_CAPS`, and the Shipyard keeps its own narrower table in `lib/shipyard/uploads.ts` (images 25MB, PDF 50MB, MP4 200MB, text 2MB) · `presignPut` signs against the global allowlist, so a type the Shipyard takes has to be in it; checkpoint 2 is photographs of hand-drawn screens and half of them arrive off an iPhone as HEIC · The Shipyard table is consulted first and is stricter, so widening the global list does not widen what a checkpoint accepts · Rejected: a second presign path that bypasses the Forge's caps, and refusing HEIC, which would make the design checkpoint a conversion exercise.
- 2026-09-15 · Pre-flight runs heuristics FIRST and calls a model only when they cannot decide (`lib/shipyard/reviewer/preflight.ts`) · SPEC §6 describes pre-flight as a cheap-tier model call on every submission, but three of its four jobs are not model work — link liveness is a HEAD request through safe-fetch, text extraction is a parser, near-duplicate detection is a shingled Jaccard over prior submissions — and the fourth, blank-or-spam, is unambiguous for all but a handful; an ordinary submission therefore costs zero pre-flight tokens, and the §6.5 budget (~$21 across 7,000 pre-flight calls) still holds as an upper bound we spend under rather than over · The model can only ever ADD a blank or spam finding, never talk the heuristics out of one, and a failed pre-flight call degrades to the heuristics instead of failing the review · Rejected: an unconditional call (pays for 7,000 answers to a question already answered 6,900 times) and dropping the model half entirely, which loses the one case the heuristics genuinely cannot call.
- 2026-09-15 · "Blank" means EVERY required text field is under 20 characters, not any one of them · A product name is legitimately ten characters long, so the obvious reading would have marked most idea submissions blank; one short field among several full ones is a bad answer, which is the reviewer's job to return with a reason, not pre-flight's job to intercept · Rejected: a per-field floor that varies by field kind, which puts a second, invisible bar beside the published one.
- 2026-09-15 · A `pass` that needs a human is recorded as a pass and does NOT clear the gate: `decideOutcome` returns `{ verdict: "pass", needsHuman: true }` and the pipeline holds the checkpoint closed until an instructor resolves it (`clearsGate`) · The reviewer's judgement is worth recording honestly even when it cannot be trusted alone, and rewriting it to `return` would tell the student they failed something the reviewer thought they had passed; the student's spine shows the review as pending rather than either outcome · The rules that set it: confidence below 0.7, any contradiction, a `needs_human`/`spam`/`unsafe_content` flag, every rubric score at or above 95 on attempt 1 or at or below 5 on any attempt, a pass that marks a clause unmet, and a 50-point score jump from a returned attempt into a pass · Rejected: downgrading such a pass to a return (dishonest, and it burns one of the student's attempts) and passing it with a warning, which is the quiet pass SPEC §5 says this checkpoint must not have.
- 2026-09-15 · The reviewer's render sandbox is default-deny cross-origin with the hostname resolved and checked BEFORE a browser exists (`lib/shipyard/reviewer/render.ts`) · SPEC §8 requires network limited to the target origin; the implementation resolves the student's hostname through safe-fetch's `isPrivateAddress` and refuses private, loopback, link-local and mixed public/private answer sets without launching Chromium at all, then aborts every request in `page.route("**/*")` whose host is not the target host, a subdomain of it, or one of five named public asset CDNs (fonts.googleapis/gstatic, jsdelivr, cdnjs, unpkg), counting the blocks so a missing third-party widget is never held against the student · Deliberately lighter than `worker/jobs/screenshot-capture.ts`, which proxies every byte through `safeFetchResource`: this render must EXECUTE the student's app against its real origin to get past the empty shell, and a fulfil-proxy changes the page's own network semantics · Rejected: `--no-sandbox` (the Forge does not pass it and the worker image does not need it) and allowing any CDN, which reopens the egress the allow-list exists to close.
- 2026-09-15 · Visible text under 80 characters after hydration is reported as an empty shell, in the render's notes and again in the prompt · That is exactly the failure SPEC §6 invokes the browser to catch — a build error, a blank body or a permanent spinner — and naming it in both places means the `loads` criterion fails on evidence rather than on the model's impression of a short page · Rejected: failing the render outright, which loses the screenshot the student needs to see what we saw.
- 2026-09-15 · The reviewer sees at most 5 images, each at most 1.5MB raw, with the checkpoint-3 screenshot first · SPEC §6.5's verdict budget assumes a handful of images; twelve phone photos at four megabytes each is a different cost curve and a worse prompt. `sharp` is not a dependency of this repo, so the downscale is a runtime probe (a variable import specifier, so TypeScript does not demand the types) and an oversized image is skipped LOUDLY — the note tells the reviewer not to hold its absence against the student — rather than silently dropped or silently sent · HEIC is refused with the fix in the note ("ask the student to upload it as a JPG") instead of failing the review · Rejected: adding `sharp` as a dependency for one cap, and sending everything, which would blow the per-call budget on deadline night.
- 2026-09-15 · `PROMPT_VERSION` ("2026-09-15.1") is baked into the system prompt and stored in `ShipyardReview.promptLog` · A review read six months later has to be legible against the prompt that produced it, and the fixture-agreement numbers in this file are attached to a version rather than to a date; the system half is otherwise byte-identical for every submission of a checkpoint, which is what makes `cache_control` on it worth sending · Rejected: a git SHA (changes on every unrelated commit) and no version at all.
- 2026-09-15 · The fixture set is 55 cases: 10 for each of the five model-reviewed checkpoints and 5 informational ones for `workflow`, which `pnpm eval:reviewer` reports as skipped rather than sending to a model · Each checkpoint carries at least four clear passes and four clear returns that fail DIFFERENT criteria (a parked waitlist, 3 signups from the student's own cohort, a job story that is a feature list, 25 claimed against 3 shown, an empty shell, a landing page with no product, a Google Form standing in for a dashboard, "pricing coming soon", cost-plus reasoning, a launch written in the future tense, bought signups) plus two borderline cases where a careful human could go either way · Four cases carry real generated PNGs so the vision path is genuinely exercised; the rest describe their images in prose, which the harness passes through labelled as a description · Rejected: fixtures generated by a model (they agree with the model that wrote them) and a pass-heavy set, which measures nothing about the returns students actually get.
- 2026-09-15 · `callStructured` gained an optional `models` override, used ONLY by `pnpm eval:reviewer` · Agreement is reported per model, and a number is meaningless if the routing table's in-request fallback answered half the calls; production paths leave it unset and keep the `[primary, fallback]` array SPEC §6.5 requires · Rejected: a second HTTP path for evals, which would have made the eval measure code students never run.
- 2026-09-15 · The demo persona picker at `/shipyard/demo` renders only when `isTestLoginEnabled()` and `notFound()`s otherwise · It is a list of ten seeded accounts anyone can become with one click, which is exactly right for a demo stack with no real people in it and exactly wrong anywhere else; a 404 rather than a 403 for the same reason `app/api/shipyard/files` uses one — a 403 confirms the page is there. The ids live in `lib/shipyard/demo-personas.ts` and the page checks each against the database before offering it, so a reseed that reshuffles the buckets shows a dead name rather than a login that half-works · Consequence: `app/shipyard/layout.tsx` resolves the session but no longer enforces it (one page under /shipyard needs no session, and a layout that redirected would redirect that page to itself); the spine, the grade line, `/instructor` and `/admin` each enforce their own · Rejected: a route group to lift the demo out of the layout, which would have moved four working files to buy the same thing.
- 2026-09-15 · Staff hitting `/shipyard` are redirected to `/shipyard/instructor`, and the shell's nav for staff drops "Spine" and "Grade" · The student spine is one person's own data; the read-only copy of it for staff is the drill-down, which shows the rubric-adjacent detail (confidence, cost, model, the render) that a student must never see. The previous "Staff view" notice on the spine was a second, weaker rendering of the same information with a warning attached · Rejected: keeping the notice — two screens answering "how is this student doing" drift, and the one with less on it is the one nobody maintains.
- 2026-09-15 · A manual open is a per-student `ShipyardCheckpointState.manuallyOpenedBy` mark consumed by `resolveGates`, never a state write · `recomputeGates` stays the only writer of gate state (architecture §4), so the escape hatch cannot put a student into a combination the rule would not produce: a manually-opened checkpoint that is then cleared behaves exactly like any other cleared checkpoint, and the one after it opens on its own. `POST /api/shipyard/instructor/open-gate` stamps the mark, calls `recomputeGates`, and writes an `AuditLog` row (`shipyard.gate.manual-open`) whose `after.reason` carries the required, minimum-eight-character reason · Rejected: writing `state = "open"` directly, which would have been undone by the next tracker sweep.
- 2026-09-15 · The instructor matrix polls `/api/shipyard/instructor/matrix?sectionId=&ifVersion=` every 8s; the student spine stays at 4s · A student watching their own gate is waiting for one specific thing to move and four seconds is the difference between "instant" and "did it work"; an instructor watching a section is reading a shape, and eight seconds keeps a wall display honest without asking Postgres for 360 cells fifteen times a minute per open tab. Both use the same `ifVersion` content hash, so a quiet section costs one sha1 rather than a page of JSON · Rejected: SSE for the matrix, which is a connection per open tab for a screen nobody stares at.
- 2026-09-15 · `GET /api/shipyard/exports/matrix?sectionId=` is name, email, section, product, then state / passed-at / attempts per checkpoint · The state answers "where is this person", the date answers "when", and the attempt count answers "how hard was it" — the three questions a section head actually opens a spreadsheet for. The state column reports what the student is HOLDING inside an open gate (`returned`, `in review`) rather than only the gate, because "open" covers both "has not started" and "is three attempts in". No grades and no PCI: neither is computed yet, and neither leaves this portal when it is · Rejected: one column per checkpoint with a packed string, which is unsortable and unpivotable.
- 2026-09-15 · A FINALISED Shipyard grade is never recomputed: `computeProductGrade` reads the row first, and if `provisional` is false it returns it untouched and reports `skipped` · A grade is derived from live inputs — the tracker's numbers keep moving after the course ends, a re-review can land weeks later, and a weights change re-scores the cohort — so "provisional until faculty sign it" is only meaningful if signing actually stops the number from moving; the recompute routes count a finalised grade as skipped rather than failing, so an admin sweeping a section is not blocked by one student who was finished early · The escape hatch is deliberate and loud: an admin flips `provisional` back on through the finalise audit trail, which is one more AuditLog row, rather than through a silent recompute · Rejected: recomputing and warning on drift (the number a student was given would still have changed underneath them), and refusing the recompute call outright (an admin sweeping 480 products would have to know which 12 were signed).
- 2026-09-15 · Activating a new `ShipyardWeights` version re-scores every PROVISIONAL grade and no finalised one, in batches of 25 up to a cap of 500, after the write has committed · Weights are config (SPEC §7) and a config change that left 480 stale grades behind would mean the grade line and the active weights disagreed until something else happened to a student; doing it outside the activation transaction keeps a tracker read off a write lock, and the cap means "save weights" cannot become an unbounded request · Versions are history, not slots: reusing a version label is a 409, because a grade row that says `weightsVersion: "v1"` has to be readable against the split that produced it · Rejected: recomputing inside the transaction, and recomputing lazily on next read (a grade nobody opens would never be re-scored, and the instructor matrix reads grades in bulk).
- 2026-09-15 · A checkpoint edit that changes `gateType` or `metricSignals` sweeps every product's gates immediately; every other edit (bar, rubric, deadline, cooldown, field schema) changes nobody's gate and sweeps nothing · Those two fields are inputs to `resolveGates`, so editing them silently changes the answer for all 480 students at once — a money checkpoint that gains a required signal has to close for the students who no longer meet it, now, not at the next fifteen-minute cron tick · The sweep is capped, batched and counts its failures instead of raising, because the gate-sweep job is the backstop and a checkpoint edit must not fail because one product's tracker read timed out · Rejected: always sweeping (an admin fixing a typo in a bar would recompute the cohort), and never sweeping (the editor would be a promise the portal keeps fifteen minutes later).
- 2026-09-15 · The Shipped.money refresh callback (`POST /api/shipyard/tracker/refresh`) authenticates with a bearer token AND an HMAC signature AND a ±300s timestamp window, all three compared in constant time, and every failure is the same uniform 401 · It is the one Shipyard route with no Clerk session — the caller is a server — so the bearer alone would mean a leaked token is a permanent write into 480 students' gate states; signing `${timestamp}:${slug}` with the same key means a captured request cannot be replayed after five minutes or re-aimed at another student's project. It is deliberately the SAME scheme `lib/tracker/real.ts` signs its own reads with, so one shared secret covers both directions and there is one thing to rotate · The route is 503 when `SHIPPED_MONEY_SERVICE_TOKEN` is unset (an unconfigured callback is off, not open) and is the only Shipyard path added to `proxy.ts`'s public list · Rejected: a bearer token alone, a shared HMAC with no expiry window, and distinguishing "bad token" from "bad signature" in the response.
- 2026-09-15 · A tracker read that returns null changes NOTHING: `refreshTrackerForProduct` returns the stored checkpoint states rather than passing null into `recomputeGates` · `resolveGates` reads null signals as "every metric signal is false", so a five-second network blip on Shipped.money would have flipped a cleared money gate back to open for everyone refreshing at that moment, and a student would watch a checkpoint they had passed close in front of them · The stamped dates (`passedAt`, `metricClearedAt`) were already write-once for the same reason; this extends the rule to the state itself · Rejected: treating an unreachable tracker as all-false (that is what the bug was) and retrying inside the request (a deadline night does not want 480 requests each holding a retry loop open).
- 2026-09-15 · The workflow-run count is read from n8n by this portal (`lib/n8n/`) and folded in by `lib/n8n/merge.ts` ONLY when Shipped.money's own `workflowRuns` is null — SPEC §8.5 item 2's fallback, taken because Shipped has no n8n source yet · The tracker wins the moment it reports a number, including zero, so the day Shipped ships its source this adapter falls silent without a deploy and without a config change; the merge lives in `lib/shipyard/tracker-refresh.ts` rather than inside `lib/tracker`, so "what the tracker said" is never confused with "what we worked out ourselves" and the tracker client stays thin · Which workflow to count comes from an optional `n8nWorkflowId` field on checkpoint 5's schema — a lookup key, not evidence: that checkpoint is metric-only, nothing typed on it is reviewed, and the count still comes from n8n, so the rule that no student-typed field feeds a metric signal holds · Rejected: adding a signal source to `lib/tracker` (two things would then claim to be the tracker), and gating checkpoint 5 on a student-reported count.
- 2026-09-15 · DPDP export and delete cover the Shipyard by extending the Forge's existing machinery, not by forking it: the Shipyard's rows are deleted explicitly and counted inside `applyDatabaseCleanup`, and its S3 keys join the same exact-version intent phase via `lib/dpdp-erasure-shipyard.ts` · Those rows would in fact cascade away with the User row, and that is exactly the problem — a cascade deletes rows without deleting the objects they point at, and an erasure receipt has to say what it deleted; Shipyard uploads store a bare key rather than a key+versionId, so the versions are resolved with `listObjectVersionIds` during the intent phase and every live version is verified absent before a single row goes · The export carries the whole record including each review's `promptLog`, because SPEC §6 requires us to KEEP the prompt and response, and a subject-access request asks for what we hold rather than what we would publish · Rejected: a separate Shipyard erasure path (two receipts, two write barriers, two ways to be half-done) and relying on the cascade (silent, uncounted, and it leaks every screenshot).
- 2026-09-15 · The Praxy export's Shipyard branch carries the product, the cleared checkpoints with their dates, and badges — and never a grade, a component, a rubric score, a payment total or a customer count · The Forge's standing invariant is that grades and PCI never leave the LMS; a badge is a validation, a boolean fact that something demonstrably happened, and `first-paying-customer` is honest on those terms because the launch gate cannot clear without a verified paying customer and no blocking flag (SPEC §5), so clearing it IS the validation while the number stays here · `tests/shipyard-admin-dpdp.test.ts` asserts the payload's shape and greps the serialised JSON for every forbidden key, so a future field cannot leak a number by accident · Rejected: sending the grade "for context" and sending the tracker's verified totals as evidence behind a badge.
- 2026-09-15 · A `pass` the trust rules flagged is HELD: `completeReview` records the review honestly (verdict `pass`, `needsHuman` true) but leaves the submission `in_review`, the gate shut, and tells the student "Your submission met the bar and is with a reviewer for a final check" · SPEC §6 says a low-confidence, contradictory or outlier verdict is "auto-queued for human review before the pass counts", and the two obvious readings of that both lie: downgrading it to a `return` puts words in the reviewer's mouth and sends a student to fix work that may be fine, while passing it and hoping a human notices means the gate has already moved by the time anyone looks · `recomputeGates` already counts only `verdict: pass, needsHuman: false` reviews, so the gate rule needed no special case; `humanResolve` in `lib/shipyard/review-escalate.ts` is the only thing that moves it afterwards · Rejected: a `held` submission status (a fifth state every UI would have to learn, when `in_review` already means exactly this), and writing the pass and reversing it if a human disagrees.
- 2026-09-15 · `workflow` — the one checkpoint no model judges — gets an INFORMATIONAL review: a zero-cost row with `modelUsed: "none"`, verdict `pass`, every criterion met, and a note saying the gate is cleared by the tracker's run count · The write-up is real work a student did and the review row is where a submission's history lives, so recording nothing would leave checkpoint 5 looking unreviewed forever; sending it to a model would spend money on a verdict that cannot move the gate, and recording a `return` would tell a student their work was rejected when the gate is simply waiting on n8n · The gate is unaffected either way: `recomputeGates` forces `reviewHalfCleared` false for a `metric` checkpoint and decides from signals alone. A non-metric checkpoint that is somehow not model-reviewed gets the same row with `needsHuman` true, because that combination is a mis-configuration and an instructor should see it · Rejected: skipping the review row entirely, and a sixth `ShipyardVerdict` value.
- 2026-09-15 · A student may dispute a `return` ONCE per review, and the dispute runs the escalation second opinion with their note in the prompt · SPEC §6: "a `return` a student disputes is escalated the same way." The cap is per REVIEW rather than per student or per hour because an appeal is about one specific verdict — a second appeal against the same verdict hands the instructor nothing new — and a student who resubmits gets a new review and a new appeal with it, so the limit can never trap anyone · Enforced by the presence of `promptLog.dispute` rather than a counter column: the note has to be stored anyway, and a counter would be a second source of truth for the same fact · Rejected: a time-window rate limit (an appeal is not spam-shaped), and letting a dispute re-run the verdict tier, which would let a student re-roll the dice.
- 2026-09-15 · Every Shipyard model call writes a `CostLog` row IN ADDITION to the model, tokens and USD already on the `ShipyardReview` · They answer different questions and neither can answer the other's: the review row knows which checkpoint a call bought and is the audit record for one verdict, while `CostLog` is the only place a pre-flight classification or an escalation second opinion appears at all, and those have no review of their own. A cost meter built on one source would either under-count the term by two tiers or lose the per-checkpoint dimension · `lib/shipyard/review-costs.ts` reads both and labels which number came from where, so the totals can be reconciled against SPEC §6.5's per-tier budget table rather than merely believed · Rejected: a Shipyard-only cost table (a third shape for the same fact), and dropping the review's own cost columns, which would make an exported review row incomplete.
- 2026-09-15 · `POST /api/shipyard/admin/router/simulate-byok-failures` exists, is admin-only, and refuses to run in production unless `ENABLE_TEST_LOGIN` is set · SPEC §9's definition of done includes "forcing five BYOK failures flips the router to flash-everywhere and logs it", and a demo that mocks the kill-switch demonstrates the mock; this route feeds five `byok-credit-or-auth-error` outcomes through `applyByokOutcome`, the same pure reducer the gateway uses, so what the room sees is the real switch · It is also a route that can silently move every review onto a different model, which has no business existing on the service students use — hence the same fence the rest of the demo affordances sit behind · Rejected: a seed flag (invisible in the room), and leaving it permanently enabled behind the admin role alone.
- 2026-09-15 · `lib/ai/openrouter.ts` sends `reasoning: { max_tokens: 2000 }` and defaults `max_tokens` to 8192 · Both models in the routing table think before they answer and OpenRouter bills the thinking out of the same budget as the JSON; left uncapped, GLM 5.3 Flash spent an entire 8,192-token budget deliberating over a checkpoint's bar and returned an EMPTY `content` with `finish_reason: "length"`, which downstream is indistinguishable from a malformed reply and burns the one retry for nothing · Disabling reasoning outright is refused by GLM's endpoints ("Reasoning is mandatory for this endpoint"), so the fence is a cap rather than a switch; an empty content now raises an error naming the finish reason and the reasoning-token count, so the next person raises the budget instead of rewriting the prompt · Rejected: raising `max_tokens` alone, which pays for the same loop at a higher price.
- 2026-09-15 · Cost per call is OpenRouter's `usage.cost` when it is greater than zero, then `usage.cost_details.upstream_inference_cost`, then the price table · Under BYOK OpenRouter reports `cost: 0` because IT charged nothing — the spend is real and lands on the Anthropic credit — and the eval run confirmed `is_byok: true` on this key, so a meter trusting `usage.cost` alone would show every Haiku review as free while $0.04 of credit went out of the door · Rejected: always computing from the price table, which throws away the caching discounts and promotional rates the provider already accounted for.
- 2026-09-15 · The Shipyard worker serves a minimal `node:http` liveness endpoint on `PORT` (default 8080), answering 200 `{ok,service,queues}` once pg-boss has started and 503 before · The Railway service inherits the web service's healthcheck path (`/api/health`) and a process with no HTTP listener fails it, so every deploy was marked FAILED while the worker was in fact draining the queue happily · Answering on every path is deliberate: the healthcheck path is configured on the web service and this process must not care which one it inherits. The server closes in the SIGTERM drain, so a draining worker stops claiming to be healthy · Rejected: a framework (a dependency for twelve lines), and changing the healthcheck path per service, which would have to be remembered on every new service.
- 2026-09-15 · M2.5, THE RELEASE GATE. `pnpm eval:reviewer` ran live against both models on the 55-fixture set (50 model-reviewed cases across five checkpoints; the five `workflow` cases are reported as skipped because no model judges them), prompt version `2026-09-15.1`, temperature 0, each model pinned with no fallback array. **The gate passed.** Numbers, verbatim:

  | | `z-ai/glm-5.3-flash` | `anthropic/claude-haiku-4.5` |
  | --- | --- | --- |
  | agreement with expected verdicts | **94%** (47/50) | **96%** (48/50) |
  | idea | 90% (9/10) | 100% (10/10) |
  | design | 90% (9/10) | 90% (9/10) |
  | working | 90% (9/10) | 100% (10/10) |
  | money | 100% (10/10) | 90% (9/10) |
  | launch | 100% (10/10) | 100% (10/10) |
  | mean confidence | 0.83 | 0.88 |
  | queued for a human | 17/50 | 16/50 |
  | tokens | 129,174 in / 32,979 out | 138,901 in / 136,941 out |
  | cost for the 50 cases | **$0.0231** | **$0.8236** |

  Inter-model agreement: **94%** (47/50). Thresholds are 85% with expected and 80% between models; both are clear. The operator's key turned out to have an Anthropic BYOK attached after all (`usage.is_byok: true`, `usage.cost: 0`), so Haiku's $0.82 is the upstream Anthropic spend read from `cost_details` rather than an OpenRouter charge — the run cost roughly $0.85 all told, $0.02 of it on OpenRouter credits.

  **Decision: the verdict tier's primary stays `z-ai/glm-5.3-flash`.** The rule set before the run was to flip to Haiku only if it agreed with the expected verdicts by five points or more; it agreed by two, inside the noise of a fifty-case set, and the two models disagree with each other on three cases out of fifty. Against that, Haiku costs **35.7×** more on identical work — $0.0231 against $0.8236 — because it spends four times as many completion tokens thinking. Flash is also the better judge on the two checkpoints that carry money (`money` 100% against 90%, `launch` 100% both). `lib/ai/router.ts` is unchanged; `flash-verdicts` remains the default profile and Haiku remains the pre-flight and escalation tier, which is where the Anthropic credit is worth spending.

  The five disagreements, one line each:
  * `design-10-implied-payment-step-borderline-return` — BOTH models pass it. The fixture returns on `flow-complete` because nothing in the path says where the CA syllabus and the student's position in it come from; both models took the generous reading the fixture's own notes concede ("the whole syllabus is fixed by ICAI and the app can default it"). The expectation is a considered editorial call about where the bar sits, not an error, so the fixture STANDS — this is the set doing its job. If it recurs, tighten `flow-complete`'s `passThreshold` rather than move the case.
  * `money-08-saree-tracker-pro-tier-unshipped` — Haiku passes a price whose Pro tier does not exist yet; Flash catches it. A charge-for-the-unbuilt case, and the checkpoint where Flash is the stronger reader.
  * `working-10-articleship-planner-borderline` — Flash passes a gap list that omits a flaky PDF export sitting on the core path; Haiku catches it. Deliberately borderline.
  * `idea-09-tiny-sample-high-conversion-borderline` — Flash returns it on `waitlist-live`, asking for proof the page loads. It is RIGHT to ask: that criterion's threshold reads "the pre-flight liveness check reached it", and the reviewer is never shown the result — see the next entry.
  * `idea-01` and `design-01` were fixture faults, fixed before this run: see below.

  Two fixtures were repaired rather than argued with, because both models returned both and both were right. `idea-01-tiffin-route-clear-pass` claimed a traffic test running to 10 September and 246 visits while its attached PNG is dated 3 September and reads 268 views; the prose now matches the screenshot. `design-01-tiffin-route-flow-pass` described a seven-screen route planner beside a photograph of a six-screen bill-to-UPI flow; the notes now describe the product that is actually drawn. Both fixture files carry a dated `FIXED` line in their own notes. The full run output is committed at `fixtures/shipyard-reviewer/last-run.json` and `last-run.md`.
- 2026-09-15 · KNOWN GAP, logged rather than patched: pre-flight's link-liveness results never reach the verdict prompt · `assembleReviewContext` builds the prompt at step 4 and `runPreflight` runs at step 5, so `linkStatuses` land in `ShipyardReview.promptLog.preflight` and nowhere the model can read them — while `waitlist-live`'s passThreshold tells the reviewer "the pre-flight liveness check reached it". The eval found it: Flash returned `idea-09` asking for proof the page loads, which is the correct response to being asked to judge a clause with no evidence for it · Not fixed in M2.5 because moving the probe ahead of the prompt changes `PROMPT_VERSION`, and a prompt change is a release-gate change (SPEC §1 rule 8) — it would invalidate the numbers above and need a fresh run. The fix is one reordering in `runReviewPipeline` plus a `<link_status>` block in `buildVerdictPrompt`, and it should land with the next gate run rather than after it · Rejected: shipping the reorder and recording numbers from a prompt that no longer exists.
- 2026-09-15 · `passed` is one-way: the stored `ShipyardCheckpointState.passedAt` goes back into `resolveGates` as `alreadyPassed`, and a checkpoint listed there is passed whatever today's signals say · `recomputeGates` re-decided every gate from scratch on every run, so a refund that dropped `payingCustomers` to zero, a product briefly disconnected from the tracker, or a null read reaching the resolver at all un-passed a checkpoint and re-locked every checkpoint behind it — a student would watch three gates shut in front of them because a webhook was late. `lib/shipyard/tracker-refresh.ts` worked around exactly this by returning the stored rows on a null read; the rule now lives in the resolver, so every caller gets it · A null read is 'no new information': metric halves that have not cleared stay where they are and the reason becomes `tracker-unreachable` rather than `awaiting-metrics`, which would report a number we never saw. A blocking flag still makes every metric signal false for a gate that has NOT passed (SPEC §5), and does not reopen one that has — the flag is surfaced where it belongs, on the grade's distribution component and in the instructor queue, not by confiscating cleared work · Rejected: guarding the write in `recomputeGates` alone (the pure function would still be wrong, and it is the thing the seed, the admin sweep and every test reason about), and an expiring pass, which would mean a checkpoint could be taken back by a number that moved after a student had already moved on.
- 2026-09-15 · The root `railway.json` is deleted; each Railway service carries its build and healthcheck settings on its service instance · Railway applies a root config file to every service sharing the `lms` root directory, and config-as-code is deprecated on this workspace so a per-service file path can no longer be set — the new Shipyard worker was silently built from `Dockerfile.web` and its start command died in an image with no tsx · `forge-prod` already had `Dockerfile.web` and `/api/health` on its instance, so nothing changes for Course 1 · Rejected: `RAILWAY_DOCKERFILE_PATH` (verified to lose to the file), a combined web+worker image, and a separate Railway project (Clerk production is bound to lms.praxel.in).
- 2026-09-15 · A HELD PASS reads "Met the bar. With a reviewer for a final check." on the spine — not "in review" — with no queue position, no cooldown and nothing to resubmit · The submission row is genuinely `in_review` (DECISIONS, 2026-09-15, the held-pass entry) but the two facts that word carries for a student are both false here: the model is finished, and there is nothing they can do to move it along. A queue position would be a number that predicts nothing, and a cooldown would imply work to redo. `SubmissionView.heldPass` is set in `lib/shipyard/spine.ts` rather than derived in the component, so the poll hash, the page and the drill-down cannot disagree about which sentence a student is being shown · The card keeps its one accent on the "open now" chip; the held-pass eyebrow is the mint surface, because two lime chips two lines apart is one accent too many · Rejected: a fifth submission status (the data layer already refused one, and the UI should not invent it either), and showing the queue position anyway "for consistency".
- 2026-09-15 · The dispute is ONE-SHOT and looks like it: a quiet `<details>` disclosure under a returned verdict, and once it is sent the control is gone for good · The server allows exactly one dispute per review, so a control that stayed on screen would only be a way to earn a 409; and a student reading a return should be reading the fixes, not weighing an appeal, which is why it is a disclosure rather than a button. `DisputeForm` deliberately does NOT call `router.refresh()` on success — the dispute sets `needsHuman`, a refresh re-renders the panel with `pendingHuman` true, and the same guard that hides the control would take the confirmation with it, so the student would watch the form vanish with no idea whether it went · A returned verdict already flagged for a person shows no control at all: the appeal has either been made or is moot · Rejected: an always-visible "Dispute" button (it invites the reflex it should discourage), and re-wording the server's 409 in the client, which would mean two sentences for one rule.
- 2026-09-15 · The admin bench is five URL tabs (`?tab=`), each loading only its own data, rather than one page or client-side panels · An admin sends "look at this" as a link and expects to land where the sender was, and a bench that forgets the panel after every write makes you re-find your place; the tab is also the only thing that decides whether the page pays for `loadCostMeter`'s four aggregates or `listCheckpoints`' six rows. The cost panel labels its two sources rather than reconciling them — model and feature read `CostLog`, by-checkpoint reads the verdict cost on the review — because they legitimately do not add up and a screen showing both without saying so reads as a bug · The queue is a list of cases, not a table of rows: the four things that decide what to do about a flagged review (why it was flagged, what the student was told, what the student said back, what a second model thought) are paragraphs, and paragraphs do not go in cells · Rejected: a single scrolling bench (five heavy panels on one request), and client-state tabs.
- 2026-09-15 · The 14-day cost chart is hand-written inline SVG in a server component, not a chart library · It is one series of fourteen numbers; a library would make the admin bench a client component, ship a runtime for a polyline, and re-import its own opinions about grid, type and colour beside a design system that already has all three. The axis labels are Plex Mono like every other figure in the portal, the grid is recessive, the line is the only coloured mark on the panel, and a per-point `<title>` is the hover layer a static SVG can honestly offer · No legend, because with one series the caption names it · Rejected: Recharts or similar (a client boundary and ~50KB for a sparkline), and a PNG rendered server-side, which cannot be hovered, selected, or read by a screen reader.
