# Revenue reconciliation fixtures

**Suite:** S5-REV-FIXTURES-v1  
**Pack:** S5-WP-REV-03  
**Order:** independent cases; use a fresh run namespace for each.  
**Safety:** synthetic references and integer minor-unit amounts; no money or ledger action.

The timeout case represents a frozen source pull failure. The private evaluator compares classes, minor-unit totals, approval state, and action counts with its secure expected-result contract.
